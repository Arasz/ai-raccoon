# adr/

Architecture decision records: immutable, frozen. Each file records one decision in date
order (`NNNN-slug.md`); records are never edited after acceptance — a new decision gets a
new number. Add an ADR for any architecture-level decision via `create-task-spec` /
`owner-gate-review` workflow.

## Contents

| ADR | Decision |
|-----|----------|
| [0001 — FluentValidation in the core](0001-fluentvalidation-in-core.md) | Domain validation via FluentValidation |
| [0002 — OpenTelemetry observability](0002-opentelemetry-observability.md) | BCL-only Meter/ActivitySource on all MCP tools |
| [0003 — Source file as first-class citizen](0003-source-file-first-class-citizen.md) | `source_file` column + weighted FTS source index + source identity on results |
| [0004 — Dual-vector structure signal](0004-dual-vector-structure-signal.md) | heading-path storage + structure embeddings fused at fixed α for section-targeted retrieval |
| [0005 — Source-affinity ranking](0005-source-affinity-ranking.md) | adjacent-chunk boost (λ) + source consolidation + document-first tie-break over the fused list |
| [0006 — RRF parameter optimization](0006-rrf-parameter-optimization.md) | 96-point sweep re-confirms k=60, 1:1, minScore 0.0, window max(3×,100) as the grid optimum; window becomes a `SearchQuery` parameter |
| [0007 — Propose tier](0007-propose-tier.md) | Persisted per-project waiting-for-promotion queue (`promotion_queue`) with fair-share capacity (cap ÷ project count reservations, borrowing, eviction of the biggest occupier's weakest row); propose persists, promote consumes from the queue; every tool response enveloped as `ApiEnvelope(data, meta, result)` with waiting-promotion meta; metrics on a dedicated Meter |
| [0008 — Live PID discovery for monitoring](0008-live-pid-discovery-for-monitoring.md) | `GET /observability` on the serve HTTP port returns the live server's PID (and OTLP status) so the CLI can fill in the README's `<server-pid>` placeholder, correct under attach, with no PID file |
| [0009 — OTLP export](0009-otlp-export.md) | OpenTelemetry SDK adopted, opt-in on `OTEL_EXPORTER_OTLP_ENDPOINT`, exporting the existing `AiRaccoon.MemoryTools`/`AiRaccoon.PromotionQueue` instruments plus the built-in `System.Runtime` metrics over OTLP; `project_id` exported in plaintext (ADR 0002's hashing item retired, not implemented); supersedes ADR 0002's OTLP non-goal on owner instruction |
| [0010 — Bank maintenance](0010-bank-maintenance.md) | WAL checkpoint at every process boundary + on a timer, VACUUM/ANALYZE on a longer cadence, both settings-table configurable |
| [0011 — Schema versioning](0011-schema-versioning.md) | Records the gap (no `PRAGMA user_version`, per-feature existence probing instead) and the chosen direction (a `user_version`-keyed migration ladder); implementation is a separate work item |
| [0012 — SSH-key derivation → HKDF](0012-ssh-key-derivation-hkdf-replacement.md) | Replaces the hand-rolled `SHA-256(label ‖ seed)` bank-key composition with platform `HKDF-SHA-256`; existing Bitwarden/SSH-keyed banks are rekeyed via the `ai-raccoon encryption migrate` verb (shipped in #99) |
| [0013 — Extension host hook surface](0013-extension-host-hook-surface.md) | Drops the never-dispatched `OnSweepAsync`/`OnConsolidateAsync` hooks from `IMemoryExtension`; sweep and consolidation stay observable through `OnDeleteAsync`, which `SweepService`/`WorkspaceService` already trigger; superseded by ADR-0016, which removed the extension host entirely |
| [0014 — Settings never cross the sync boundary](0014-settings-never-sync.md) | Settings (cloud credentials, embedding endpoint/key) are per-machine; push strips them from every pushed snapshot and pull never reads `remote.settings`, gated by tests on both directions |
| [0015 — Retrieval gates assert portable bands](0015-retrieval-gates-assert-portable-bands.md) | Golden-file comparison moves from positional/1e-6-exact to set-based hashes within a 5e-3 ranking tolerance plus k-boundary absorption; rank pins become ceilings; A6 band widens to the measured cross-platform envelope; the osx-arm64 skip is removed so gates run on every platform |
| [0016 — Remove the extension host](0016-remove-the-extension-host.md) | Deletes `IMemoryExtension`/`MemoryExtensionHost`/`RetrievalRatingExtension` and `OnSourceChangedAsync` entirely — the sole registered extension never overrode the one hook still dispatched in production, so the pipeline was reachable but inert; supersedes ADR-0013 in full and reverses spec-issue-1 §6.2 |
| [0017 — TensorPrimitives in AiRaccoon.Core](0017-tensorprimitives-in-core.md) | Adds `System.Numerics.Tensors` as `AiRaccoon.Core`'s second third-party dependency and vectorizes `EmbeddingMath.MeanPoolAndNormalize` with `TensorPrimitives`; ships only if WP-4's benchmark shows a clear win, else reverted and withdrawn |
| [0018 — Promotion scoring v2](0018-promotion-scoring-v2.md) | Replaces `SharedExtractionService`'s four additive bonuses (measured +0.125 Spearman) with `PromotionScorer`: a 14-archetype provenance prior, bounded content-shape evidence, and an organic-entry refinement layer; `CandidateFloor` recalibrated 1.0 → 0.4; recency drops from the score to a sort tie-break only |
| [0019 — Forward-version write guard](0019-forward-version-write-guard.md) | `MemorySchema.EnsureAsync` refuses a read-write open when the stored schema version is ahead of the binary's `CurrentVersion`, closing the silent-no-op path that caused issue #200's chunk-column drift; recovery is updating the binary, not a downgrade or override |
| [0020 — Always-on HTTP: the stdio entry point becomes a proxy](0020-always-on-http-stdio-proxy.md) | Bare `ai-raccoon` becomes a thin stdio→HTTP proxy that probes for `ai-raccoon serve`, starts one if absent and forwards every JSON-RPC message to it, so one process owns the bank instead of N peers; `--transport stdio` keeps the in-process server as the escape hatch; reverses the "no self-spawning daemon" position and supersedes R15/R16; ships with a loopback token on `/mcp` |
| [0021 — Export the ASP.NET request span](0021-export-the-aspnet-request-span.md) | Registers `Microsoft.AspNetCore.Hosting` so the request span is exported and tool spans nest under it, fixing every exported span being an orphan with a dangling `parent_span_id`; takes the `SuppressActivityOpenTelemetryData` AppContext switch over a fourth OTel package; tool spans stay `Internal` against the MCP convention so two nested `Server` spans do not double-count request rates; supersedes ADR-0002 §Non-Goals b2 and ADR-0009 §Non-Goals b1 and moves ADR-0002 to Superseded |
| [0022 — `serve --restart` over an authenticated loopback shutdown](0022-authenticated-loopback-restart.md) | `serve --restart` cycles the ai-raccoon server already on the port instead of attaching to it, closing the `dotnet tool update` lockout ADR-0020 left open: a token-guarded `POST /shutdown` (mapped only on a gated host, POST-only, answered before the host stops) plus a bounded wait for the port to free; `/observability` gains the binary version as the discriminator; every failure is a loud non-zero exit and `--restart` never falls back to attaching; no process is ever killed by PID |
| [0023 — Invalidate promotion_queue rows when their entry is deleted](0023-promotion-queue-entries-delete-invalidation.md) | An `AFTER DELETE ON entries` trigger (`promotion_queue_entries_ad`) drops the matching `promotion_queue` row on any entry delete, guarded by `NOT EXISTS` so a live sibling under `uq_entries_committed_bucket` keeps the candidate; no `CurrentVersion` bump — additive and idempotent; `ai-raccoon extract prune` (dry-run by default, `--apply` to delete, idempotent) catches up rows orphaned before the trigger existed |
| [0024 — Unknown ids: idempotent removal reports a count, a state transition refuses](0024-unknown-id-contract.md) | Records the rule closing three repeated "is this a bug" sweeps: `memory_delete`/`memory_delete_context`/`memory_promotion_discard`/`memory_watch_remove` are idempotent removal verbs that return `0`/no-op on an unknown id; `memory_share` and the workspace family are state transitions that refuse an unknown id with a typed exception; no behaviour change, both sides already matched the rule |
| [0025 — The sweep reaper](0025-the-sweep-reaper.md) | Retroactive ADR for the 1.6.0 default-on background reaper: on-by-default kept (pre-1.6.0 banks were already inert against it, no TTL ever set); `sweep.enabled`/`sweep.threshold` stay bank-global, not per-project; the reaper now honours a project's access mode (skips non-`full` projects) closing the gap where `memory_sweep` refused in `ro` but the timer reaped anyway; a pass that deletes something finally emits a span, a pass where every project failed finally reports a non-`success` result; the fail-open kill switch parse is kept as-is and flagged for a future call, not changed here |
| [0026 — Persistent discards and shared-value exclusion](0026-persistent-discards-and-shared-exclusion.md) | `RankAll` builds the shared-value set whitespace-stripped, exactly mirroring `PromoteAsync`, so an exact twin or a whitespace twin of an already-shared value is never proposed again |
| [0027 — Extensible file-type handlers and JSON support](0027-extensible-file-type-handlers-and-json-support.md) | Three focused `AiRaccoon.Core` interfaces replace the markdown-only ingest path, so a new file type is a handler registration rather than a change to `FileIngestor` |
| [0029 — Pre-write noise filtering](0029-pre-write-noise-filtering.md) | `INoiseFilteringService` runs inside `SqliteMemoryStore.WriteAsync`, rejecting machine-generated content before it reaches the bank; superseded in part by ADR-0033 and ADR-0039 |
| [0030 — Real-time heuristic TTL assignment](0030-realtime-heuristic-ttl.md) | An `IAutoTtlPolicy` pipeline injected into `SqliteMemoryStore` assigns a TTL at write time; reversed by ADR-0034, which found the assignment never reached the database |
| [0031 — Polly resilience pipelines](0031-polly-resilience-pipelines.md) | Polly v8 (`Polly.Core` + `Microsoft.Extensions.Http.Resilience`) standardizes every HTTP resilience policy on exponential backoff with decorrelated jitter, replacing per-client hand-rolled retries |
| [0032 — A write outcome is truthful](0032-truthful-write-outcome.md) | `MemoryEntry` gains additive `Stored`/`Reason` members rather than a new return type, so a rejected write says it was rejected instead of returning a fabricated entry that no `memory_get` could ever resolve |
| [0033 — Remove the zero-shot noise filter and noise learning](0033-remove-zero-shot-noise-filter-and-noise-learning.md) | Deletes the zero-shot filter and the learning subsystem feeding it, keeping only the deterministic `HermesProcessNoisePolicy`; superseded by ADR-0039, which restores the substrate without a scoring model |
| [0034 — An explicit TTL is authoritative](0034-explicit-ttl-is-authoritative.md) | No heuristic assigns a TTL at write time: `PromotionScorerTtlPolicy` and `IAutoTtlPolicy` are deleted as dead code that described and tested behaviour which never reached the database; reverses ADR-0030 |
| [0035 — `memory_get` and query-relevant snippets](0035-memory-get-and-query-relevant-snippets.md) | Adds `memory_get(projectId, hash)` with the same reach `BumpAccess` already grants search hits, plus query-relevant snippets and content-keyed dedup, closing the gap where no tool could retrieve an entry by hash |
| [0036 — Engine-aware chunk token budget](0036-engine-aware-chunk-token-budget.md) | Chunks are counted with the tokenizer that will actually embed them rather than a proxy, with a guaranteed split floor so no chunk is ever truncated at embed time |
| [0037 — Workspace and promotion-queue concurrency guards](0037-workspace-and-promotion-queue-concurrency-guards.md) | A partial unique index on `entries(path, hash, workspace_id)` plus a v7 migration ladder step that dedupes first; promotion claims gain a reclaim path for stale holders |
| [0038 — Cache the resolved encryption key per config fingerprint](0038-cache-the-resolved-encryption-key-per-config-fingerprint.md) | The sidecar is still read on every resolve, so a config edit is always seen; only the provider's `GetPassphraseAsync` — the step that shells out — is skipped when the fingerprint is unchanged |
| [0039 — The noise-learning substrate and shadow mode](0039-noise-learning-substrate-and-shadow-mode.md) | Supersedes ADR-0033: restores the learner-agnostic substrate and shadow mode only, ships no scoring model, and leaves a marked seam for one |
| [0040 — Read-path query guard](0040-read-path-query-guard.md) | `memory_search` gets a two-tier guard evaluated on the raw query before any other work: refuse a query that is itself machine output, annotate one that merely contains log-like content |
| [0041 — Structural/lexical noise detector](0041-structural-noise-detector.md) | A pure shape-statistics detector (no embedding) joins the read-path warn tier as a third input, default off; it can add an annotation but never a refusal |
| [0042 — Fixtures are built by the product, not beside it](0042-fixtures-are-built-by-the-product-not-beside-it.md) | Deletes the parallel Python chunker that populated a `section` column the product never wrote, and routes the corpus scripts through `memory_ingest_file`; a fixture built by a second implementation hides the first one's defects |
| [0043 — A probe with no answer is not an empty port](0043-a-probe-with-no-answer-is-not-an-empty-port.md) | `ProbeVerdict` splits a refused connection (proof the port is free) from a probe that got no answer at all; `serve --restart` may still bind on the unknown, but a refused bind now refutes the pre-check instead of reporting a lost port nothing ever took |
| [0044 — The section column's FTS weight is 4, not 16](0044-section-fts-weight.md) | `section` carried a 16x bm25 weight while `FileIngestor` wrote it as NULL for every ingested chunk, so the weight had never met real data; measured A/B on one corpus, populating the column beats leaving it NULL and weight 4 beats weight 16 (nDCG@5 0.5703 → 0.5733 → 0.5846). Records two defects it exposes but does not fix: one `expectedSource` per query in the baseline catalog, and heading extraction reading shell comments as headings when a chunk starts inside a code fence |
| [0045 — A context is a label inside a project, not a second isolation boundary](0045-context-is-a-label-not-a-boundary.md) | A `memory_write(context: …)` landed in `scope=custom`, which search read only when the caller passed the same label back — and `memory_stats` omitted custom contexts, so the label could not be discovered; measured live, such an entry was in the bank, in FTS and readable by `memory_get` while `memory_search` returned 0 results at every scope. No `contextLabel` now means every context in the project, one means that context only, project isolation unchanged |
| [0046 — "Rows belonging to this project" has one definition](0046-project-membership-has-one-definition.md) | `scope = 'project'` was hand-copied into nine queries, a trigger and a filter — six of them naming another copy as the authority — so ADR-0045 fixed search and stats while `memory_set_ttl` and `memory_share` still answered `unknown-hash` for an entry `memory_get` returned. The rule moves to `ProjectRows`, a gate compares the copies, and the TTL tie-break keeps its preference without using an existence filter to express it |
| [0047 — The score floor is relative to the top hit, and says so](0047-relative-score-floor.md) | `minScore` was documented as an absolute 0..1 quality floor but applied to a max-normalized ranking, so it was a rank cutoff relative to each response's own top hit; measured, 0.0/0.1/0.3/0.5 return identical sets up to limit 20, an off-corpus query ("how to braise a wombat in aspic") still scores 1.000 at rank 1 and survives 0.7 untouched, and the shipped 0.7 default silently shortened 10 of 44 baseline queries at limit 20. Renamed `minRelativeScore`, defaulted to off, description states the trap; an absolute relevance signal is named as the unbuilt follow-up |
| [0048 — A chunk is a well-formed markdown fragment](0048-a-chunk-is-a-well-formed-markdown-fragment.md) | ADR-0036 de-fenced any code block it could not keep atomic, so a chunk could begin inside one and `HeadingPathParser` read its `#` shell comments as headings — 70 of 4,126 chunks on this repo's docs ended in a different fence state than they began. An over-budget fence is now re-fenced into bounded fences and a never-closed one is closed, so every chunk opens and closes its own fence state; measured on the corpus sources, 4 fabricated sections go and 13 real headings come back |
| [0049 — The bundled model's embeddings depend on the host CPU](0049-embeddings-depend-on-the-host-cpu.md) | Six identical CI jobs from one commit split exactly on `avx512_vnni`: VNNI hosts measure AdrNdcg5 0.4886, non-VNNI x64 hosts 0.5588, macOS arm64 0.5261 — a 0.070 spread against a 5e-3 tolerance. The bundled model is u8s8-quantized, so ONNX Runtime's saturating `VPMADDUBSW` and non-saturating `VPDPBUSD` produce genuinely different query vectors; retrieval order is a function of the host CPU, and the two sweep gates were pinning the arm64 one |
| [0050 — The sweep gates search with pinned query vectors](0050-sweep-gates-search-with-pinned-query-vectors.md) | ADR-0049 left the two retrieval sweeps pinning a number no Linux runner produces. Their query vector was the one input still computed per run, so `tests/AiRaccoon.Tests/Resources/gate-query-vectors.json` commits one vector per catalog query and a throw-on-miss `PinnedQueryEmbeddingService` feeds them to the sweeps; the corpus vectors were already fixed. `AdrNdcg5` re-pins 0.526 -> 0.5260827785380623 — the same number, now a property of the fixture rather than of the host, with the 5e-3 tolerance unchanged. Nothing in `src/` moves and the re-quantize/fp32 product question stays open; the fixture records which arithmetic path and date produced it, and gates keep it matched to the catalog and to `BundledModel`'s shas |
| [0051 — A context never names another project, on any path](0051-a-context-never-names-another-project.md) | `7698dc63` fixed a write that named another project by guarding `EntryBucket.For`; the delete path's copy of the same context-to-rows mapping, `FilterFor`, had neither the guard nor a test, bound `project_id` from the caller-supplied string, and left `shared` with no project predicate — so one `memory_delete_context` call emptied another project and one more wiped the shared tier, both exploited against a real server at the `full` mode the deployed bank already runs. `ContextScope.RequireWithinProject` becomes the single decider and both paths call it; `FilterFor` moves to `ContextFilter.cs` beside `EntryBucket` so the two halves of one rule stop living in different files, and the store's size ratchet is LOWERED 1291 -> 1251 rather than raised. A direct `memory_write` to `shared` stays allowed — that is an open owner decision, not a security fix |
| [0052 — The workspace lifecycle is a write, not a destruction](0052-the-workspace-lifecycle-is-a-write-not-a-destruction.md) | `memory_workspace_consolidate` and `memory_workspace_discard` required mode `full`, so an agent could open a sandbox at the default `rw` and then neither finish nor abandon it — which pushed installs to `full` just to use workspaces, manufacturing the precondition for ADR-0051's cross-project delete. Neither reaches committed memory: consolidate promotes into the caller's own project, and discard's filter binds both `workspace_id` and `project_id`. Both are now gated `Write`; `ro` still refuses them, pinned by a test. `Destructive` keeps its meaning — reaching committed memory. Automatic sweeping is unaffected: `SweepHostedService` never goes through the tool gate |
| [0053 — `rating` is computed where it is stored](0053-rating-is-computed-where-it-is-stored.md) | `BumpAccessAsync` read a row, computed `RatingPolicy.Rating` in C#, then wrote it back — so `access_count` (a relative expression) survived interleaving and `rating` (a literal) did not, leaving the sweep's deletion input drifting below the entry's own access history. Folded into the `UPDATE`, which is atomic by construction and removes a round trip rather than adding a transaction. Worth reading for the reproduction: the first attempt passed against the defect at 200 concurrent searches, because Microsoft.Data.Sqlite has no true async I/O and `Task.WhenAll` over a `Select` never interleaves — `Task.Run` reproduced it at 64. Deletes `SelectRatingForBump` and `RatingRow`; store ratchet lowered 1251 -> 1243 |
| [0054 — A default that answers wrongly is worse than no default](0054-a-default-that-answers-wrongly-is-worse-than-no-default.md) | `IMemoryStore.GetAsync` defaulted to "not found" for an entry that exists, `DeleteInScopeAsync` defaulted to the unscoped `DeleteAsync` it exists to prevent, and `IPromotionQueueStore.ClaimAsync` defaulted to the delete-based `DiscardAsync` — each justified as sparing an implementor a compile error, which is exactly how an implementor should be told. All three are abstract; the compiler named six relying implementors, all test fakes, none production. Each now declares the member explicitly with the same behaviour: a fake may simplify, a port may not lie |
| [0055 — A discard is load-bearing while its entry lives](0055-a-discard-is-load-bearing-while-its-entry-lives.md) | `promotion_discards` (965 live rows) and `search_quality` (424) had no reaper anywhere in `src/`, while `noise_entries` had one the pattern was never generalised from. Both are now age-bounded in the maintenance pass behind settings — 180 and 90 days. The discard purge requires age **and** an absent entry: while the entry lives, the discard is the only thing stopping the propose path re-offering a candidate the agent already rejected, so age alone re-inflicts the rejection |
| [0056 — A retrieval gate measured off its tuning set](0056-a-retrieval-gate-measured-off-its-tuning-set.md) | The headline metrics gate asserted `ShouldBeInRange(0.0, 1.0)` on nDCG/MRR/recall — values those functions return for any input — and the same 11 queries both selected and gated the ADR-0005/0006 parameters. The plan's leave-one-family-out control is unavailable here: the corpus has two generators (`docs/` and `.ai-badger/`) and the tuning set spans both, so the partition is taken at the document instead, leaving A8/A9/A10 held out. Measured on one path: **in-sample 0.673, held out 0.285** — out-of-sample retrieval is 42% of the published figure. The discrimination proof runs on the mean, because reversing the ranking *improves* A8 (0.131 → 0.491), so its own floor is one a reversal survives |
| [0057 — The headless penalty is load-bearing](0057-the-headless-penalty-is-load-bearing.md) | Records a change **built, measured and rejected**. `structureSim ?? 0.0` caps the 64% of rows with no structure embedding at half what a headed row can reach, and ADR-0004 claims the opposite ("degrade to content-only") — true only for a uniformly headless bank. Scoring absent structure as content-only instead regresses S3 3→4, S4 3→6, S6 3→10, A2 1→2 and held-out A10 0.170→0.146, and inverts ADR-0056's reversal probe (0.610 reversed vs 0.282 unreversed — the ordering becomes anti-correlated). The cap is the mechanism by which the dual-vector signal favours headed chunks at all; without it a headed row wins only when `structureSim > contentSim`. `StructureFusion` unchanged; the test that asserted the behaviour is re-adjudicated with the evidence, and a new gate pins the property |

| [0058 — The second fusion is order-preserving, and its removal is not yet measurable](0058-the-second-fusion-is-order-preserving-and-its-removal-is-not-yet-measurable.md) | Records a change **built, measured four ways and not shipped**. `Merge` re-fuses an already-fused list, rebuilding every score from rank position, so a strong match set and near-orthogonal junk both leave as `1, 0.98387, 0.96825`. But at λ=0 the code with and without the second fusion is **byte-identical** — `(k+1)/(k+rank)` is strictly decreasing, so re-fusing a sorted list preserves order. Its only effect is compressing the score range that ADR-0005's two swept constants were calibrated against, so deleting it silently re-scales them: held-out 0.2846 → 0.2571 alone, → 0.3333 with the sibling floor restated as its equivalent rank window, → 0.2818 with both constants restated. The derivation applies to both, and that row is inconclusive; picking the 0.3333 row would be tuning on a 3-query held-out set. Blocked on held-out capacity, not on owner question 7 (answered: do both). Three characterisation tests pin the defect and go red when it is fixed |

| [0059 — The layering guard the repo had already paid for](0059-the-layering-guard-the-repo-had-already-paid-for.md) | `TngTech.ArchUnitNET.xUnitV3` was pinned in `tests/Directory.Packages.props` and referenced by nothing, so every architecture finding of the 2026-08-14 review was invisible to CI at 0 warnings. Three rules, each watched fail first. **Rule 2 was vacuous on the first attempt and passed against a violation already known** — ArchUnit's `NotDependOnAny(Types()...)` matches only types loaded into the architecture, and the target set was empty; the working form reads each type's own dependency list, with a second test asserting the scanned set is non-empty. It then found `ResiliencePipelineFactory` (HTTP retry policy) in Core, which moves to Infrastructure and takes Core's `Polly.Core` dependency with it. Rule 3 found **12** concrete injections across all 8 tool classes, not the predicted 8 — and every one already had an interface, so the fix is parameter types and nothing else. Subsumes WP14 and unblocks its owner question |
| [0060 — An unrecognised verb must not launch anything](0060-an-unrecognised-verb-must-not-launch-anything.md) | `CliArgs.TryParse` reported success whenever the *option* read succeeded, so parse errors were rendered to stderr and then ignored and the run launched the proxy anyway — against the DEFAULT port and token file, so a command scoped to a scratch `--data-root` reached into the production install (recorded as a checklist failure on both 1.13.0 and 1.14.0). A parse error on a launch path is now fatal, exit 9. The guard sits AFTER the verb branch on purpose: a known verb whose own argument is wrong keeps `ExitCode.InvalidArgument` (15), the distinction that lets a script tell "you mistyped" from "the bank is broken" — the first attempt collapsed the two and an existing test caught it, so the guard moved rather than the test |

| [0061 — An unmapped exception must say what it was](0061-an-unmapped-exception-must-say-what-it-was.md) | `ToolRefusals.Filter` had no final catch, so anything it did not map escaped to the SDK and reached the client as "An error occurred invoking '&lt;tool&gt;'." — eleven words carrying neither the type nor the message, identically for an agent, an operator and a CI log. That is why `ToolRefusalsTests`'s five intermittent reds were undiagnosable, and it answers the consumer-surface lane's open question: an unmapped exception becomes a generic protocol error with nothing in it. Now answered as `unexpected-error: <Type>` — type only, message stays server-side — and logged at **Error** here (event 912) rather than relying on the rethrow, since catching means the SDK never sees it. `ArgumentOutOfRangeException` is still not laundered into a refusal. First half of WP19; the race is only tractable once the failure says what threw |

| [0062 — A fake clock advanced before its timer exists is lost](0062-a-fake-clock-advanced-before-its-timer-exists-is-lost.md) | WP19 half 2. The plan blamed `LoopbackPort` contention across four flaky tests; `IdleWatchdogTests` binds no port and holds 18 `Task.Delay` calls where `ToolRefusalsTests` holds zero, so they are two defects wearing one symptom. The watchdog's race: `StartAsync` returns before `ExecuteAsync` creates its `PeriodicTimer`, so a clock advance inside the `Task.Delay(100) // timer registers` window is **not observed late, it is lost** — the timer schedules against the new now and the loop waits for a tick a frozen fake clock never delivers. Reproduced by setting the delays to zero (2-3 of 8 fail every run). Fixed by waiting on observables — timer registration, tick count, `StopCalls` — never on the clock; 15 s → 90 ms, and the negative assertions stop being false-passable. Five more files carry the latent pattern. `ToolRefusalsTests` is NOT fixed: it survived three clean runs and one under ten busy loops on ten cores, disconfirming CPU contention as its trigger |
| [0063 — Chunk to the engine that will embed, not the one configured](0063-chunk-to-the-engine-that-will-embed-not-the-one-configured.md) | WP3 step 2, one of two code defects behind blocker B2. `FileIngestor.ChunkSizeForAsync` fell back to the default budget and the **o200k** counter whenever `embedding.provider` was unset, while the bundled model tokenizes BERT WordPiece. Configuring an engine later re-embeds the bank but **never re-chunks** it, so ingest-then-configure — a supported order — wrote permanently-wrong boundaries that truncate at embed time while the row still reads `embed_state='embedded'`. An unset provider now resolves to the bundled engine: chunking to the most restrictive plausible window is safe in the direction that matters, since a chunk fitting 254 BERT tokens fits any larger window. Refusing to ingest until configured was rejected — it breaks a supported order for no extra safety. Watched red at **23 of 37 chunks over-window, worst 295 tokens** |
| [0064 — `memory_write` chunks like everything else](0064-memory-write-chunks-like-everything-else.md) | WP3 step 1, the other code defect behind blocker B2 and the one with no operational workaround. `memory_write` did **not chunk at all** — the only budget-aware chunking lived in `FileIngestor` — so a long body stored as one row and embedded only its first window while still reading `embed_state='embedded'`. Live: **555 rows, 320 over-window, 114,883 tokens never embedded**; watched red at a 29,200-char body storing as **1 row of 8,123 tokens** into a 256-token window. Now routed through the same chunker and budget resolution as file ingest. The path still derives from the whole body so a document keeps one identity; the returned entry addresses chunk 0 via a new hash-scoped lookup, because `WHERE path = @path LIMIT 1` was unspecified the moment a path meant several rows. `SqliteMemoryStore.cs` **shrank** 1283 → 1238 (insert moved to `WriteChunks.cs`), ratchet **lowered** not raised |
| [0065 — The tool layer holds no pipeline](0065-the-tool-layer-holds-no-pipeline.md) | WP8/H22. `mcp-thin` was an invariant nothing checked, so `ShareTools.ShareExtract` grew to **51 body lines** (median 9) holding a consent gate, a mode decision and two pipelines reachable from nowhere else — in a class whose own comment says "no business logic here" — and `EvaluateQueryGuardAsync` became a tiered policy engine inside the tools file. A size gate over `[McpServerTool]` bodies named both, then `ShareExtractService` and `QueryGuardService` moved to Core. Three forced decisions: Core has **no logger**, so the guard returns a `Shadowed` verdict for the host to log (event 920 stays put — the id names the event, not the file); `confirm-required` became a domain exception mapped to the same wire prefix; and four tests asserting `McpException` were re-pointed at `ToolRefusals.PrefixFor` because they were asserting the mechanism, not the contract. Cap set at 40 where the code honestly lands, not 30 — shrinking `Search` by hiding lines in a private method would move the number without moving the logic. **Unblocks WP2** |
| [0066 — The env gate had writers but no readers](0066-the-env-gate-had-writers-but-no-readers.md) | WP19's flake, traced. ADR-0061's diagnostic named it on the next CI failure: `unexpected-error: SqliteException` with **SQLite error 26, 'file is not a database'**, hitting three services in one server at once — which is what SQLite returns opening a **plain** bank **with** a key. `TestData.EnvVarGate` already existed and its own comment names the hazard, but it is taken only by classes that **write** `AIRACCOON_DB_PASSPHRASE`: it serialises writers against each other and does nothing for a class that merely reads the environment by opening a bank. Reproduced on command (`AIRACCOON_DB_PASSPHRASE=… dotnet test --filter ToolRefusalsTests` → the same Error record), then that class takes the gate as a reader. **Containment, not the cure** — the cure is a seam over the environment read so tests stop mutating process-global state; the residual is stated: any new bank-opening test that skips the gate reintroduces it, and nothing enforces that yet |
| [0067 — Naming `shared` asks for promotion; it does not perform one](0067-naming-shared-asks-for-promotion.md) | `memory_write(context: "shared")` wrote straight into the shared tier at the default `rw` — **138 live rows across 5 projects**, each crossing the project boundary with no review. The first draft proposed refusing it; the owner's steer is better and this follows it: naming `shared` is **the strongest promotion signal available**, better than any scorer's inference. The write now lands in the caller's own project (searchable immediately, never lost) plus one candidate reasoned `agent-requested-share`, and the response says so. The enqueue is a Core service because `PromotionQueueService` already takes `IMemoryStore` — store → queue → store is a real cycle, and a third service composing both ports is not; **this is why WP2 waited for WP8**. Rejected the one-line `EntryBucket` fix: it closes the boundary silently, dropping the agent's request with nothing in the response to say so |
| [0068 — `ctx` is a vec0 metadata column, not a partition key](0068-ctx-is-a-vec0-metadata-column-not-a-partition-key.md) | vec0 chunks are fixed-capacity, costing the live bank **43.4 MB** of allocation beyond what its vectors need. The first draft blamed the 1024 `chunk_size` default and would have reclaimed **2% of what it promised** — **98% of the waste is the `ctx` partition key** (20 partitions, 13 holding under 10 rows). Measured, not assumed: demoting `ctx` to a metadata column takes chunk bytes 31.5 MB → 4.7 MB and costs **+1.4 ms per KNN**; coarsening to `scope` is **dominated** (same size, worse latency) and was struck. Demote, never remove — the first measurement used a table with no `ctx` at all, which returns the **global** top-k and is not a replacement. Ladder step **v9**; supersedes only the `partition key` half of the 2026-08-08 KNN plan. The +1.4 ms is invisible in production until the store times its own phases, which is why the performance-observability design exists |
| [0069 — A backfill re-chunks the row it has, not the file it remembers](0069-a-backfill-re-chunks-the-row-it-has-not-the-file-it-remembers.md) | **6,240 of 17,219 rows (36.2%)** held more text than the 254-token embedding window, worst at **7,376 tokens — 29×**, with **377,431 tokens** outside their own vector. Re-ingesting would have made it worse: `IngestFileAsync` only INSERTS, and re-chunking changes hashes, so it would have **added** 6,240 rows beside the stale ones. The plan preferred document-level re-chunking and argued it well; rejected because **109 of 1,283 source files are gone**, 33 rows have no source at all, and re-reading a changed file silently rewrites content. So the backfill splits each row's own STORED value. Validated on a copy: 6,240 → **0** over-window, text preserved (+704,033 chars from overlay); 110 pieces deduped on the bucket index, which is why the arithmetic does not close. The gate is a PAIR and each half was watched fail — deleting rows makes "nothing over the window" pass perfectly |
| [0070 — Maintenance is a list of jobs with a ledger in the bank](0070-maintenance-is-a-list-of-jobs-with-a-ledger.md) | The owner's question — *"we reclaim size on this machine, but what for other users?"* — measured out to **nobody, including us**. Ladder step v9 frees **42.0 MB** into SQLite's free list and the file does not shrink: 183,099,392 bytes before and after. Only VACUUM returns it (**−53.2 MB**, and it costs **0.6 s**, which is why the default cadence moves **7 days → 2 hours**). Nothing reliably vacuumed, because the clock was an in-memory field seeded on first pass and every MCP server is a short-lived process — its own comment admitted *"short-lived processes never vacuum"*. Maintenance becomes a job list with a ledger in the bank; `null` interval = once ever, which is why the ledger stores a timestamp not a boolean. A failed job is **not** stamped, so it retries rather than retiring unrun. Deleted `RunOnce_VacuumNotDue_OnFirstTick_SeedsTheClock` — it asserted the defect. Found a latent bug proving a job ran: **Dapper infers `CommandType.StoredProcedure` for whitespace-free SQL**, so a bare `"VACUUM"` never executed |
| [0071 — A query is trimmed deliberately, and said so](0071-a-query-is-trimmed-deliberately-and-said-so.md) | A live `warn ...[414] Chunk truncated ... 407 tokens` on a running server, with **no stored entry over the window** — 0 of 24,674 rows above 254 tokens, the largest being a 12,952-char markdown table that tokenises to **249**. The 407 tokens were a **search query**: `Encode(string)` is one choke point every embed call funnels through, so the generator cut the query silently and called it a chunk. Three defects in one line — the caller was never told, the message named the wrong thing, and it made WP3's *"EventId 414 is zero"* criterion **unmeasurable**, since queries could never let it reach zero. The query path now trims before the generator (`TokenBudget.Trim`, lifted to Core) and reports event **416**; 414 becomes stored-content only, so each is countable. Both messages rewritten for the person reading the log. Does **not** make a long query work — 407 still becomes 254, just knowingly |
| [0072 — A term budget for long queries is not adjudicable](0072-a-term-budget-for-long-queries-is-not-adjudicable.md) | Records a change **specified, measured and not shipped**; `FtsQueryNormalizer` is unchanged. The long-query path ORs `rawTokens` **with duplicates**, and repeating a term multiplies its `bm25` contribution **linearly** — two rows that tie at one copy each (−1.2238) separate to −3.6713 vs −1.2238 at three. The live bank's largest query is **448,900 chars → 49,267 raw tokens, 3,847 distinct** (92.2% duplicates), its top ten terms holding **25.9% of all term slots** and **37,429 slots** matching corpus vocabulary against the question's ~7. FTS5 takes a 50k-term OR without error, so there is no crash to prevent. Measured on the FTS leg with real pasted noise: `current` scores **0.0000 on every query, every noise size, both orderings**; dedup alone moves held-out 0.0000→**0.1200** at best and is unchanged in 37 of 38 pairs; a small cap recovers ground **only when the question comes first**, and the briefed claim that it does is **false for about half** the real long queries (`'},'`, `'"value": "1.1 varnish"'`, `[ASYNC DELEGATION BATCH COMPLETE]`). The 3-query held-out column swings 0.3919→0.0437→0.2409 with no optimum — fitting noise on n=3, as in ADR-0058. **All 44 catalog queries are ≤10 tokens with no duplicates**, so the gates are blind to any cap ≥11: they prove non-regression and nothing else. Latency deliberately **unrecorded** (load 26–60 throughout). Rejected with reasons: first-N, most-selective, dedup, dedup+cap, and trimming FTS to 254 to match ADR-0071 |
| [0073 — A write embeds the chunk it stored, not the document it came from](0073-a-write-embeds-the-chunk-it-stored.md) | Found in production by the log line ADR-0071 rewrote hours earlier: event 414 fired with **818 tokens** on a live server while the bank held **zero** rows over the window (24,707 entries, measured). Both true at once — `WriteAsync` chunked the content, stored `chunks[0]`, then embedded **`request.Content`**, the whole document, into that row's vector. **The text was chunked and the vector was not**, and every WP3 check looked only at stored text so all of them passed. One-argument fix. Makes 414 meaningful again, so WP3's *"count is zero"* criterion is measurable for the first time. Pre-1.19.1 rows are NOT repaired — their text is in budget, which is all `chunk-backfill` inspects |

| [0074 — A capped buffer satisfies the channel rule, and reshapes G4](0074-a-capped-buffer-satisfies-the-channel-rule-and-reshapes-g4.md) | The metrics writer is a capped `ConcurrentQueue` behind `IMeasurementRecorder`/`IMeasurementBuffer`, not the spec's literally-named `Channel.CreateBounded` + `DropWrite` + `itemDropped` machinery — same observable contract (bounded capacity, drop-and-count, no caller-thread write), deferred mechanism swap (D2) is a drop-in behind the port. G4 (spec.json: *"count bank writes; assert zero"*) can never pass as written — `BumpAccessAsync` writes on every search and `total_changes()` is per-connection — so it is reshaped to count `metrics` table rows with the flusher paused; spec.json still states the old, unimplementable form |
| [0075 — Only the server writes to the bank](0075-only-the-server-writes-to-the-bank.md) | A profiling pass asked why `memory_search` cost what it did, and the first hypothesis — *nothing pools* — was **wrong**: `Microsoft.Data.Sqlite` pools by default, `BuildConnectionString` never disables it, and `efcore#28774` was fixed in 6.0.11 against our pinned 10.0.11. What made an open expensive was what happened *on* it — `EnsureAsync` ran the whole `Ddl` block every time, **upstream of the `storedVersion >= CurrentVersion` early return at `:414` that existed to prevent exactly that** — multiplied by `SqliteSettingsStore` opening a fresh bank per settings row. Measured with a real `sqlite3_trace` hook, and the plan's estimates were wrong in three places: the block is **39 statements (42 for the whole call), not "~30"**, and it costs **4.5 bank opens per operation, not 3.60**. After the digest gate: **4 statements**, pinned on both sides — 4 when the digest matches, 39 when it is stale. Fixing the arithmetic left the structural question standing — *why is a CLI process writing the bank at all?* Two writers to one SQLite file was never a decision; it accumulated one command family at a time. The owner's steer settled it: **all settings operations go through the server**, the CLI surface is untouched (*"the CLI command will stay, just the logic"*), the many settings families collapse into one `settings` command, and the server auto-starts via the existing `BackendLauncher`. Rejected: a read/write split across processes (leaves every future operation needing a judgment call about which half it is), and a settings cache (gated behind cross-process liveness tests, since a cache that goes stale across processes costs more than the reads it saves). `encryption` moves too — and that is a **correctness** fix, not a performance one: `RekeyBankAsync` calls `ClearPool`, which is **process-local**, so a CLI clearing its own pool does nothing about the connections the server still holds against the bank it just rekeyed |
| [0076 — `model set` is an outbox, drained by an on-demand relay](0076-model-set-is-an-outbox-drained-by-an-on-demand-relay.md) | Closes issue #358 (ADR-0075 §10.3, unruled): `model set` was the last CLI-direct write. The owner ruled no progress channel, lock every bank operation for the duration, and the re-embed must survive a crash mid-way. First draft was a two-step status record (write started, migrate, write finished); corrected mid-implementation to the **transactional outbox pattern** — in one transaction, `model set` writes the new engine settings, a `model_migration` row, and marks every stale row `pending`, so no crash can produce the settings change without the re-embed it owes, or vice versa. A second correction removed an inline re-embed the first draft still ran synchronously inside the request: starting and finishing a migration are separate responsibilities, so `model set` now returns as soon as the transaction commits, and a relay (`ModelMigrationJob`, an on-demand `IMaintenanceJob` — `HasWorkAsync` is new, since the runner's clock-gated `Interval` would otherwise run it once and never again) drains it. `ToolGate` refuses every bank operation while a migration is open — the lock and the completion guarantee are the same mechanism, so a permanently failing migration is a permanently refusing server, by design. `SqliteModelMigrationLease` (stale-reclaiming, mirrors `SqliteWatchScanLease`) stops two relays draining the same row. `BankMaintenanceHostedService` gained a second, independent poll loop (15s, hardcoded) so the on-demand relay's backstop latency is bounded by seconds rather than the heavy pass's hourly cadence, without changing that cadence for anything else — pinned by a regression test proving an hourly job's own `Interval` is unaffected by how often the poll now wakes. `CliWriteOptOuts` reduces to `encryption` alone: ADR-0075's write-exclusivity invariant is reachable for the first time. Watched fail for real: a live server killed between the outbox commit and any relay pass, and a fresh server's startup pass alone finishing it; then, with `ModelMigrationJob` removed, the identical interruption left the bank stuck open for the full 45s bounded wait — no error, no crash, nothing noticing |
| [0077 — Table chunking is not adjudicable on a table-blind corpus](0077-table-chunking-is-not-adjudicable-on-a-table-blind-corpus.md) | Records a change **specified, evidenced and not shipped**, raised by issue #367. The chunking hypothesis is probably right and currently unmeasurable, and those are separate facts. **The corpus cannot see tables**: `RealWorldCorpus.cs` — the 68-query ParityGate corpus named as the non-regression side — contains **zero pipe characters**; the held-out sources `A8`/`A9`/`A10` have no tables; 3 of 19 gradeable sources have any table chunk. And `HeldOutRetrievalGateTests` reads a **committed binary fixture** that re-chunks only behind an env var, a second checkout and a pinned commit, skipped in CI — so under a chunking change the floors do not move and the gate goes green **having measured nothing**. The issue's own diagnosis is also wrong: the target is not "a row packing three unrelated definitions" but a chunk mixing ~150 words of prose with a four-row table, the row itself being a clean term|definition pair — so per-cell splitting would separate a term from its definition and make the motivating case *worse*. The six proposed shapes turn out to be two questions: **correctness properties** (prose and tables never share a chunk; no header-orphaned body row — deterministic, gateable on the text alone, the same shape as ADR-0048's fence-balance gate) and **tuning questions** (whole-table / per-row / per-cell / linearized-row — needing graded evidence that does not exist). Even the correctness properties are blocked, not on evidence but on ingest: changed boundaries change `ContentHash`, and `InsertChunksAsync` only INSERTs, so a re-ingest *adds* rows beside the stale ones (ADR-0069 measured 6,240). `ReplaceFileAsync` already does replace-by-path in one transaction and is the enabling work. Carries an appendix of what does not work, including a reconciliation keyed on `(ctx, source_file)` that would have deleted `memory_write` notes and promoted rows merely *citing* a file — which `MemorySql.cs:188-190` already warns against |
| [0078 — The no-fusion-regression rule is an order, and it ships default-off](0078-the-no-fusion-regression-rule-is-an-order-and-ships-default-off.md) | Closes the fusion half of issue #367. ADR-0006 declares a **"no fusion regression" gate** — *the hybrid never ranks the expected chunk below the best single modality* — and the 25,995-entry bank is a counterexample to it: a chunk at **FTS rank 1** lands at **#18 under default hybrid**, reproduced independently at FTS 4 / vector 14 / hybrid 6. Not a bug — RRF pays consensus, so `1/63 + 1/62 ≈ 0.0320` beats a decisive `1/61 ≈ 0.0164`. The first design, `max(rrf, best_single_leg)` injected into the fused score, **does not work and is pinned as such**: `SearchResultMerger.cs:26` re-fuses from position, so a strong set (`1.0, 0.99, 0.98`) and a weak one (`0.03, 0.002, 0.001`) both leave `Merge` as exactly `1.0, 61/62, 61/63`; equal scores fall to `ThenBy(Path, Ordinal)`, so changing **only the filenames** swaps which result holds rank 1; and "raise only, never lower" is false on a total order. Only position survives, so the rule ships as a **permutation** — `key = min(fusedRank, bestRank over CONTRIBUTING legs)` — with **zero free parameters**, which is exactly why it can ship where ADR-0072 could not: that record had to pick a cap number off a 3-query held-out column and every candidate was noise-fitting, whereas this is on or off. **The offline corpus still cannot adjudicate it** (ADR-0056: out-of-sample is 42% of the published figure, held-out tier is 3 queries, one of which a *reversed* ranking improves; ADR-0058 built its change, measured four ways, refused at 0.2818 vs 0.2846) — so no nDCG figure is offered, and it ships **default-off behind `fusion.noRegression.enabled.global`**, bank-wide and deliberately **not** on the MCP surface, with the enabled path collecting the evidence a static corpus cannot be. What survives downstream is **established, not assumed**: the second fusion preserves the order exactly (ADR-0058), but `SourceAffinityRanker` at the shipped λ=0.1/k=60 overrides it by ~7 rank positions per adjacent sibling — a chunk placed **9th** with two siblings scores `61/69 + 0.2 = 1.0841` and beats the one placed **1st** at `1.0` — which is why the diff is measured on the **served** list, not the fused one. `ModalityLeg` (Core) ships regardless of the rule so a degraded leg is never read as disagreement; a finding recorded honestly: at the *ordering* level that check is redundant, because on one contributing leg every rank-based rule of this family collapses to the identity, so it earns its place on the **telemetry**, where removing it does redden a gate. Three metrics into the existing `metrics` table, each with a stated consumer and a SQL verdict recipe joining `search_quality` on correlation id (an earlier draft proposed five while its own recipe read one). Toggled by `settings retrieval fusion enable|disable|show` (ADR-0076's node-under-settings rule; `enable`/`disable` per the tree's existing boolean convention, and `show` names the default because `False` alone cannot tell an unset bank from a disabled one) — without a verb the flag would ship dark, since there is no generic `settings set <key> <value>` and ADR-0075 makes the server the only writer. Eight perturbations watched red. `SqliteMemoryStore.cs` hit its ratchet at 1144/1112 and **WP8's search seam came out instead of a raise** — cap **lowered** 1112 → 1066 |
| [0079 — Table chunking becomes adjudicable on a corpus that re-chunks](0079-table-chunking-becomes-adjudicable-on-a-corpus-that-rechunks.md) | Supplies all three things [ADR-0077](0077-table-chunking-is-not-adjudicable-on-a-table-blind-corpus.md) named as missing, and still ships **no chunking change** — `MarkdownChunker` is untouched. **A corpus that can see tables**: 13 documents vendored at pinned commits from `ai-badger` and `arasz-home-page`, both external to this repo, so no retrieval parameter has ever been swept against them — ADR-0056's failure mode addressed at the source rather than by partition — with 16 graded queries whose answers are table cells. **A gate that re-chunks**: `TableCorpusBank` ingests that markdown at test time through the production `FileIngestor`, per ADR-0042, instead of reading the committed bank that "goes green having measured nothing". **A response variable the arm cannot destroy**: a chunk is relevant iff it is from the expected document **and** carries the graded answer span. Baseline at the production 254-token budget, 343 chunks: **mean nDCG@5 0.070683, mean MRR@10 0.089658**, most queries exactly 0 — #367 reproduced across a corpus rather than one hand-picked chunk. The mechanism is measured: the answer-bearing chunk is on average only **49% table content**, and where it is prose-dominated a literal probe of the answer text scores **0.0 even though the chunk contains the exact string searched**. Non-blindness is itself a test — re-ingesting at 128 tokens takes 343 chunks to 784 and moves 4 of 16 per-query scores (T8 0.50→0.00, T14 0.63→0.39). Two findings kept rather than smoothed: **reversal is not a valid discriminator here** — a reversed top-10 *outscores* the real order (0.161 vs 0.071), because the answer chunk sits in the bottom half when it ranks at all, so the gate mispairs each query with the next query's document instead, collapsing both means to 0; and **a span anchor bounds inflation without freezing it** — the 48-token overlay copies a span into adjacent chunks, so a 6.2× finer corpus took the largest relevance set 1→3, against whole-file relevance growing 5.9×, so cross-arm comparisons must report set sizes. Motivated by table-bearing chunks being retrieved **1.48× more often** than others (10.62% vs 7.20% ever-accessed) — partly circular, but enough to make the corpus worth building |
| [0080 — The table-chunking correctness properties cost retrieval, and no arm wins](0080-the-table-chunking-correctness-properties-cost-retrieval.md) | Builds [ADR-0077](0077-table-chunking-is-not-adjudicable-on-a-table-blind-corpus.md)'s two correctness properties and scores its two surviving tuning arms on [ADR-0079](0079-table-chunking-becomes-adjudicable-on-a-corpus-that-rechunks.md)'s corpus. **The properties work**: a table region (a pipe row followed directly by a separator row, outside any fence) becomes whole-chunk units repeating the header, taking no overlay and leaving none, and the answer-bearing chunk's table share goes from a mean of **49% to 100% on all sixteen queries**. **And they cost retrieval**: mean nDCG@5 **0.070683 → 0.039433**, mean MRR@10 **0.089658 → 0.064658**. The prose that shared the chunk was carrying the match, and a bare pipe-syntax table is poor embedding material. Neither surviving arm recovers it — per-row + header carry-over lands 0.058167 / 0.051215 and a row linearised into sentences 0.058167 / 0.053199, so ADR-0077's "most promising" arm is **indistinguishable from plain per-row on nDCG@5 to six decimals**, noise rather than a result. Every arm held a mean relevance set of exactly 1.00 (max 1), so no arm gained by multiplying answer-bearing units — the mechanical inflation ADR-0077 feared and ADR-0079 could only bound, now shown absent, which is what makes the comparison believable. **Nothing merges**: the implementation stays on its branch, and ADR-0079's floors are deliberately **not** re-pinned downward, because a floor lowered to admit the change it exists to catch is not a floor — that gate went red at 0.039433 on its first encounter with a real chunking change, which is the gate working. Corrects a reading ADR-0077 invited: "not blocked on measurement" is not "free" — these properties are *decidable* without a corpus, but wanting them still needs one. Next arms named: a table chunk carrying its section heading (ADR-0077 already noted a whole-table chunk forfeits the 4× bm25 `section` weight), and a wider graded set, since 11 of 16 queries at zero is what lets two arms tie to six decimals |
| [0081 — At forty queries, two table arms beat the baseline](0081-at-forty-queries-two-table-arms-beat-the-baseline.md) | Supersedes the **conclusion** of [ADR-0080](0080-the-table-chunking-correctness-properties-cost-retrieval.md) — "no arm wins" — while keeping its measurements, which were right at the width they were taken. ADR-0080 named the risk itself ("do not act on a gap of 0.02 at this width"); acting on it changed the answer. The graded set went **16 queries / 13 documents → 40 / 22**, and the integrity gate rejected three non-unique spans plus one that had drifted into prose while the new queries were authored — three of four would have silently distorted the numbers. Then the untried arm ADR-0080 named was built: **a table chunk carrying its section heading**, motivated at the source rather than assumed (`FileIngestor.HeadingSection` parses `section`/`heading_path` out of the **chunk's own text** via `HeadingPathParser`, so a table with no `#` line gets `section = null`, can never satisfy a `file#section` anchor, and forfeits the 4× bm25 section weight ADR-0077 flagged). Against a **baseline of 0.227007 / 0.237599**: correctness properties alone still lose at 0.146341 / 0.140645 — ADR-0080's central finding survives — but **per-row + section heading returns 0.292417 / 0.275129 (+29% nDCG@5, +16% MRR@10 over leaving chunking alone)** and **linearised + section heading 0.294594 / 0.273601**, the two indistinguishable from each other and reversing order between the metrics. The heading is worth **+0.031 to +0.051 nDCG@5 on every shape** — the cheapest change in this whole line of work and the one ADR-0077 predicted from bm25 weighting without ever testing. Per-row vs whole-table was invisible at the old width (0.261 vs 0.146 here; 0.058 vs 0.039 there): same ordering, magnitude inside the old noise. Relevance sets held a mean of 1.00–1.02, max 2, so no arm gained by multiplying answer-bearing units. **Still ships nothing** — 40 queries by one author in one sitting is a tuning set (ADR-0056), the prose side is unmeasured though per-row nearly doubles chunk count, and the 910-file/15,099-chunk reingest question is open. What it fixes is the decision basis: "no arm wins" is withdrawn and the next attempt starts from per-row + section heading |

## Numbers never used

| Number | Why |
|---|---|
| 0028 | Skipped when 0029 was written; no document ever occupied it and nothing references it. Recorded so the numbering gate can tell a deliberate skip from a lost file. |
