# REVIEW R2-qa — independent review of the follow-ups plan

# QA / TEST-HONESTY REVIEW — integrated plan `doctor-feature-match-cont` (WP1-4, gates, checklist)

Independent review lane. I wrote none of the plan and edited nothing. Every claim below is either a `file:line` read in the worktree `.ai-badger/worktrees/doctor-feature-match-cont` (same commit as main post-#591) or a command I actually ran against this repo's MTP setup; the two are labelled. Reviewer standard: the R2 bar applied to #591 — prove-the-check-fails (RED witnesses real, mutations named), honest counts, anti-swap, gate commands that actually run.

## 0. Evidence I ran (first-hand, this repo)

| # | What I ran | Result |
|---|---|---|
| G1 | `dotnet test --project tests/AiRaccoon.Tests --filter "FullyQualifiedName~SettingsChannelExitCodeTests" --no-build -v m --minimum-expected-tests 3` | **total: 3, passed, exit 0** — `--minimum-expected-tests` + `-v m` works in this repo's MTP setup (global.json:6-8) |
| G2 | `--filter "FullyQualifiedName~ModelResetGuardTests|FullyQualifiedName~ModelResetGuardEndpointTests" --no-build -v m --minimum-expected-tests 4` | **total: 0, exit 8** (loud) — the bare-pipe OR form parses; the classes don't exist yet, so the gate fails loudly, never vacuously |
| G3 | `--filter "FullyQualifiedName~HowToExitTableTests" --no-build -v m --minimum-expected-tests 2` | **total: 0, exit 8** (loud) — same, WP3 gate form parses |
| G4 | `--filter "Speed=Fast&Performance!=Benchmark" --no-build --list-tests` | filter resolves (lane runs; R2 E8 counted 3312) |
| G5 | `python3 scripts/version-bump.py` argparse | `choices=("patch","minor","major")` — `patch` supported (version-bump.py:38-39) |

## 1. Ruling on the RED witnesses (question 1) — both REAL

**Integration RED — real.** Today `SettingsEndpoint.MapDelete` has no migration check: `await store.DeleteSettingAsync(key, ctx)` (SettingsEndpoint.cs:62) → `SqliteSettingsStore.DeleteSettingAsync` (SqliteSettingsStore.cs:43) deletes unconditionally. `ModelResetAsync` (SettingsCommands.cs:260-275) deletes Provider first (:270), so today `settings model reset` on a seeded-open bank returns 204 per key, prints the success line, exits 0, and the outbox row stays open — the defect. A test asserting exit 25 + nothing deleted fails today. ✓

**Fast RED — real.** Today `ModelResetAsync` has no catch; a store that throws `ModelMigrationInProgressException` propagates to `ConfigCommands`' catch-all (ConfigCommands.cs:165-169) which formats via `CliFailureFormatting.Format` → `"ai-raccoon: {ex.Message}"` (CliFailureFormatting.cs:15) and returns `ExitCode.InvalidArgument` = **15** (ExitCode.cs:45). With the frozen message (which starts `ai-raccoon: `) as the exception text, today's stderr is `ai-raccoon: ai-raccoon: model reset refused: …` — the doubled prefix claim is accurate. The harness shape is proven: `SettingsChannelExitCodeTests.ModelSetCodeLocal_OnACodeEngineActivationRefusal_ExitsInvalidArgument_WithTheReason` (SettingsChannelExitCodeTests.cs:61-86) already drives a store-thrown refusal through `CliRun.RunAsync` + `TestData.CreateConfigCommands` and asserts exit 15. ✓

One mechanism gap in the RED protocol (MINOR, F1): the Fast test asserts `ExitCode.ModelResetRefused` which does not exist until WP1 edits ExitCode.cs — the test cannot compile, and a compile error is not a witnessed RED (R2 J7/B4 bar). Fix: assert literal `25` in the test, or land the const first and note it is behavior-neutral.

## 2. The Fast dispatcher test: catch, not guard — HONEST, worth keeping, needs relabeling (question 2)

The Fast test's fake store throws on `DeleteSettingAsync` regardless of any endpoint guard, so it proves only the CLI-side error surface (catch inside `ModelResetAsync` → exit 25 → message verbatim, no double prefix). It would go green even if the endpoint guard were deleted. That is **honest** because (a) its name says "StoreThrows…", (b) the guard is separately proven by Integration test #1 through the real HTTP→endpoint→bank seam, and (c) the R2-precedented shape (`SettingsChannelExitCodeTests`) is exactly "store throws → CLI exit/message mapping". It is **worth keeping**: it is the only Fast-lane witness for the new exit code and the verbatim-message rule (the rest of WP1 is Slow), and it is the RED witness for the catch code. Do not drop it. Two corrections: (F1) literal 25; (F2) also assert stdout does **not** contain `embedding engine reset to default: no engine` — a catch that still prints the success line passes the plan's exit+message assertions.

What the Fast test (nor anything in the 4) does **not** pin: the A2 catch-**placement** ruling. A dispatcher-level `catch (ModelMigrationInProgressException)` in `ConfigCommands` keeps all 4 tests green while changing `model embedding set`-while-open from today's generic exit 15 to 25 — exactly the out-of-scope behavior change the plan rules against (plan :38). The plan's own justification — "verified: no test pins the current 15" (plan :38, :165) — is true (grep of tests: only ToolGate/outbox unit tests reference `ModelMigrationInProgress`, none on a CLI path), which means the ruling is *unpinned by design*. MAJOR (F3): add one Fast test pinning `model embedding set local` while open → still `ExitCode.InvalidArgument` (mirror `SettingsChannelExitCodeTests.cs:61-86` with a throwing `IModelMigrationStore` or a 409-stub `StubHandler`); the plan must either add it or explicitly accept the unpinned scope boundary.

## 3. The Integration harness and the "invented adapter" (question 3) — EXISTS, and the adapter is unnecessary

The harness the plan copies exists exactly as described: `SettingsEndpointTests.cs:34-49` — `McpServerSetup.CreateWebHost(new ServerConfig(0, McpTransport.Http, options) { McpToken = Token })`, real `WebApplication`, `HttpClient` with `McpTokenGate` header, `TestData.CreateTempRoot` data root, `[RetryFact]` + `IAsyncLifetime`. The new class would need the same `[RetryFact]` (Integration/ is the retry surface — plan honors it, plan :80) and, like the source, `[Integration][Slow]` traits (state them explicitly — the plan doesn't name traits for the new class, plan :78).

The "tiny adapter" will **not** trip `CliCommandsDoNotOpenTheBankTests`: that gate derives *production* leaf `*Commands` types from `ConfigCommands`' constructor and walks the *production* DI graph for a live `ISqliteConnectionFactory` (CliCommandsDoNotOpenTheBankTests.cs:103-115, 118-138) — test-side fakes are never in that graph, and WP1 changes no constructor. ✓ Safe.

But the adapter is **not invented** — it exists twice already, which the plan denies ("no existing Unit/Integration harness does this today", plan :78/:186): `SettingsChannelExitCodeTests.cs:90` (`SettingsRoutedStore : FakeMemoryStore`) drives the **real** `ServerSettingsStore` through `ConfigCommands` at argv level (Unit+Fast!), and `Integration/Setup/PerformanceCommandsRoundTripTests.cs:38` has `DelegatingMemoryStore(ISettingsStore inner) : FakeMemoryStore` feeding `TestData.CreateConfigCommands` (PerformanceCommandsRoundTripTests.cs:60,84). `FakeMemoryStore : IMemoryStore, ISettingsStore` exists (TestHelpers/FakeMemoryStore.cs:19). And the production seam the adapter mimics is `SqliteMemoryStore` (as `IMemoryStore`) forwarding settings calls to `LazyServerSettingsStore` (AppRunner.cs:209,223; CliCommandsDoNotOpenTheBankTests.cs:35-40). MINOR (F6): cite these precedents, and prefer `new LazyServerSettingsStore(_ => Task.FromResult<ISettingsStore>(new ServerSettingsStore(client, Token)))` over a hand-rolled adapter — the production class, no invented type at all. What genuinely is new in the plan's test is only the *combination*: CLI argv → real HTTP → endpoint → `SqliteSettingsStore` → file bank in one process — that part is correct and worth having.

Fixture determinism: verified. `AcquireModelMigrationLease` WHERE is `finished_at IS NULL AND (lease_owner IS NULL OR lease_expires_at < @now)` (MemorySql.cs:501-503), so seeding `lease_owner='test-holder'` + `lease_expires_at = now+1h` blocks the 15s relay ✓. One nuance the plan misses: its cited seeding precedent uses `:memory:` connections (NonDefaultDimensionMigrationTests.cs:141-144), not a file bank under a running server — the new test writes the server's live WAL bank from a second connection. Workable (short transaction), but say so and use a busy timeout, else `[RetryFact]` masks a genuine lock flake as "retry surface" (MINOR, F11).

## 4. Derive-gate phrase matching (question 4) — set half sound; phrase half has a hole on codes 1/2

Verified against the file: the table is at configure-ai-raccoon-server.md:370-378, rows 0/1/2/19/20/22/24, backticked code cells + Meaning column — exactly the doctor-reachable set {0,1,2,19,20,22,24} (ExitCode.cs:5-6,54,57,66,78; 21 `ModelDownloadFailed` is a `model download` verb, correctly excluded).

RED mutations, walked:
- **delete the 24 row** → doc set {0,1,2,19,20,22} ≠ const set → RED ✓
- **renumber 24→25** → {0,1,2,19,20,22,25} ≠ const set → RED ✓
- **reword a Meaning** → RED only if the phrase pinned is actually present in both texts. Available cross-pairs: 19 "binary's DDL" or "differs from" (doc :53-54 vs table :375 — note the *obvious* phrase "differs from this binary's DDL" is NOT contiguous in the doc comment); 20 "user_version" (doc :56-57 vs table :376); 22 "no bank file exists at the resolved path" (doc :64-66 vs table :377); 24 "schema shape is healthy" (doc :73-77 vs table :378 — "every MCP tool call is refused until it finishes" is table-only, "…until the re-embed finishes" is doc-only, so that phrase must NOT be chosen). Phrase selection is a real design task the plan doesn't pin (MINOR, F8).
- **rename the const** → compile error, not a witnessed RED; the plan lists it among "RED witnesses" (plan :68). It is *protection*, loudly — relabel it (MINOR, F7).

**MAJOR (F4) — the plan's exception note is incomplete.** The plan exempts only `Success = 0` ("no doc comment", plan :68; ExitCode.cs:80) — but **1 and 2 have no doc comment either** (ExitCode.cs:5-6). Mechanism (b) ("assert the Meaning cell contains a key phrase that is asserted against the const's doc comment in ExitCode.cs source") is therefore unimplementable for 1/2 as written: a literal implementation either throws (loud) or extracts nothing and vacuous-passes (`cell.Contains("")`), after which rewording rows 1/2 drifts with the suite green — precisely the claimed RED mutation. Fix: extend the exception note to 0/1/2 — their phrases are table-pinned literals ("HEALTHY", "the encryption key could not be resolved", "the bank could not be opened read-only"), and the source cross-check applies only to 19/20/22/24; name one phrase per code that provably occurs in both texts.

The precedent citations are real: `CorpusEngineLinesTests.HowToHealthyBankSample_MatchesTheReportContract` regex-extracts a fenced block and asserts line-by-line (CorpusEngineLinesTests.cs:149-179, via `TestData.RepoFile` ✓ exists); `ToolInventoryTests.PackagedReadme_ToolsHeading_MatchesActualToolCount` (ToolInventoryTests.cs:167-176) regex-extracts from the doc — though that specific test extracts a **heading**, not a table; the table extraction is its sibling at :179-195. The plan's description is close enough, but the new test's regex must anchor to the exit table specifically (the doc has other backticked-number tables) — under-specified (MINOR, F12).

## 5. Gate commands (question 5) — all forms verified

- `--minimum-expected-tests` is supported in this repo's MTP setup: **verified first-hand** (G1: 3 tests, exit 0). It was used by R2 the same way.
- Bare-pipe OR filter: **verified first-hand** (G2: parses, 0 tests today, exit 8 — loud, expected for not-yet-written classes). Backslash-escaped pipe is the known trap (R2 J1) — the plan's commands use the bare pipe ✓.
- `Speed=Fast&Performance!=Benchmark` resolves (G4; identical to build.yml:134) ✓.
- WP3's docs-only fast-lane claim: **TRUE** — build.yml:71 CODE_REGEX includes `^docs/how-to/`, so a docs-only how-to change sets `code=true` and build-fast runs the whole lane, including a Fast `HowToExitTableTests` ✓.
- WP4: `python3 scripts/version-bump.py patch` — `patch` is an argparse choice (version-bump.py:38-39) ✓; `VersionContractTests` exists (VersionContractTests.cs:19) ✓.
- Inconsistency (MINOR, F9): the plan's own risks table says "`--minimum-expected-tests` on every gate; never `--nologo`" (plan :143, echoing R2 N5) — but WP1 gate #2 (`Speed=Fast&Performance!=Benchmark`, plan :89) and the WP4 `VersionContractTests` gate (plan :130) omit the flag. Add it (fast lane: a loose floor such as 3000; VersionContractTests: its exact count).

## 6. Checklist rows (question 6) — arithmetic right, executability is WP2's un-gated deliverable

- Draft has 7 items (json :11-71); the plan's count is 7 + 4 R3 rows + 1 new guard row = **12** ✓ arithmetic consistent with the research's own enumeration (research :52-69).
- "12 is the right count" is defensible only against R3 block 3; two loose ends: R3's stale-lease drain row was "optionally tightened" and the plan does not tighten the drain-logs row to include the 1009 case; and R2 §7's live-stuck-bank row (scale, MCP-refusal correlation, published-tool exit 24 on the real 51,947-row bank) is still absent from the file. State the decision either way (MINOR, F10).
- Commands: the raw-SQL seed route is real (NonDefaultDimensionMigrationTests.cs:124-130 inserts `model_migration` rows by SQL; settings rows likewise :107-110). `EXIT=25` is valid only on a **1.36.1** tool — the file header says run 1.36.0/pr 591 (json :2-8); rows 8-12 need the version/derived block updated or a note, else the 1.36.0 runner records an impossible EXIT=25 (MINOR, F10b). The new row's exit-0 leg needs the explicit "close the row" SQL (`UPDATE model_migration SET finished_at = …`) spelled out, and the serve+seed sequence, since settings verbs require the server (ADR-0075). Pre-existing: draft row 3's command copies to `/tmp/ev-copy.db` but runs `--data-root /tmp/ev-copy-doctor` (json :32) — a path mismatch a human will trip on; the WP2 gate checks only 12 rows/7 fields/JSON parses (plan :100), so **command executability is not gated at all** — recommend a reviewer pass over the five new command strings as part of WP2's AC (MINOR, F10c).

## 7. What proof is missing (question 7) — summary

1. **Both reset verb spellings** — AC (a)/(b) claim both verbs (plan :84), no test name exercises `settings model embedding reset`. Both dispatch to the same method today (ConfigCommands.cs:58,62), so the hole is a routing regression, not the guard — but it is exactly the kind of "AC claims more than tests pin" gap R2 flagged. Fix: `[RetryTheory]` over both verb spellings on tests #1 and #2 (F5).
2. **409 body text surviving verbatim through the real HTTP hop** — the new 409 mapping in `ServerSettingsStore.DeleteSettingAsync` is pinned for *existence* by test #1's exit 25 (delete the mapping → `HttpRequestException` → catch-all → 15 → RED), but its *body fidelity* (mapping throws `ModelMigrationInProgressException(body)` not a hardcoded string) is pinned only if test #1 asserts the exact stderr text. The Fast test fakes the exception, so it cannot see this. Fix: assert `err == frozen message` in test #1 (F2/F5).
3. **Catch placement** (A2) — see §2, F3.
4. **WP3 phrase sources for codes 0/1/2** — see §4, F4.
5. **Checklist command executability + 1.36.0/1.36.1 traceability** — see §6.

## 8. Verdicts on the questions asked

1. RED witnesses: **both real** (exit 0 today: SettingsEndpoint.cs:62/SqliteSettingsStore.cs:43 have no guard; exit 15 today: ConfigCommands.cs:165-169 → ExitCode.cs:45; doubled prefix: CliFailureFormatting.cs:15). Fast RED needs literal 25 (F1).
2. Fast test tests the catch, not the guard: **honest, keep it** — relabel as the CLI error-surface test, add the stdout-negative, and pair it (as the plan does) with the Integration guard test, which is the only test that would go RED if the endpoint guard were deleted.
3. Harness exists (SettingsEndpointTests.cs:34-49); adapter pattern exists twice (SettingsChannelExitCodeTests.cs:90; PerformanceCommandsRoundTripTests.cs:38); `CliCommandsDoNotOpenTheBankTests` cannot trip on test-side fakes (it walks the production DI graph). Use `LazyServerSettingsStore` instead of an invented adapter (F6).
4. Derive gate: set-equality RED on delete/renumber ✓; phrase RED on reword ✓ for 19/20/22/24 (with careful phrase choice) and for 0/1/2 only if phrases are table-pinned literals — the plan's exception note covers only 0 (F4); "rename const" is compile-time protection, not a RED (F7).
5. Gate commands: all forms verified first-hand; `--minimum-expected-tests` works (G1); filters resolve and fail loudly when empty (G2/G3); docs-only fast-lane claim TRUE (build.yml:71); two gates missing `--minimum-expected-tests` (F9).
6. Checklist: 12 = 7+4+1 ✓; commands executable in principle; EXIT=25 valid only on 1.36.1 (traceability note); executability un-gated (F10).
7. Missing proof: verbatim message through HTTP, both verb spellings, catch placement, WP3 phrases for 1/2, checklist command strings.

# SCHEMA-LAST

## Findings

| id | severity | plan section / test | finding | file:line | fix |
|---|---|---|---|---|---|
| F1 | MINOR | WP1 RED protocol, Fast test | Test asserts `ExitCode.ModelResetRefused`, which does not exist until WP1 — the RED run cannot compile; compile error is not a witnessed RED (R2 bar) | plan :76-77; ExitCode.cs:45 | Assert literal `25` in the test (const added later, refactor-only), or land the const first and state it is behavior-neutral |
| F2 | MAJOR | WP1 tests #1 + Fast | No test as named asserts the frozen message **verbatim on stderr**; AC (a) claims it (plan :84) but the test list doesn't name the assertion. A 409 mapping that throws a hardcoded message passes exit-25-only tests; the Fast test (fake store) cannot see the real HTTP 409 body | plan :82, :84; ServerSettingsStore.cs:81-86 (no 409 branch today — the new mapping is the risk) | Test #1 asserts `err == frozen message` exactly (and stdout lacks the success line); name the assertion in the test list |
| F3 | MAJOR | WP1 A2 ruling / test set | Catch-placement ruling (catch inside `ModelResetAsync`, not the dispatcher) is unpinned: a dispatcher-level catch keeps all 4 tests green while `model embedding set`-while-open changes from 15→25 — the exact out-of-scope change the plan rules against; plan's own justification is "nothing pins the current 15" | plan :38, :84; ConfigCommands.cs:165-169 | Add Fast test `ModelEmbeddingSet_WhileMigrationOpen_StillExitsInvalidArgument` (mirror SettingsChannelExitCodeTests.cs:61-86 with a throwing `IModelMigrationStore` or 409 `StubHandler`); bump `--minimum-expected-tests` to 5 |
| F4 | MAJOR | WP3 `HowToExitTable_EachMeaning_MatchesTheConstsDocComment` | Mechanism (b) is unimplementable for codes 1 and 2 — they have no doc comment (plan notes only 0); a literal implementation either throws or vacuous-passes, after which rewording rows 1/2 stays green — the claimed RED mutation | plan :68; ExitCode.cs:5-6, :80 | Extend the exception note to 0/1/2: table-pinned literals; source cross-check only for 19/20/22/24; name one phrase per code that provably occurs in both texts (e.g. 24: "schema shape is healthy"; 19: "binary's DDL"; 20: "user_version"; 22: "no bank file exists at the resolved path") |
| F5 | MAJOR | WP1 tests #1/#2 vs AC (a)/(b) | AC claims **both** reset verbs; no test exercises `settings model embedding reset` (ConfigCommands.cs:58 vs :62 — a routing regression keeps all 4 green) | plan :82-84 | Make tests #1 and #2 `[RetryTheory]` over both verb spellings; `--minimum-expected-tests` 4 → 6 |
| F6 | MINOR | WP1 Integration harness prose | "No existing Unit/Integration harness does this today" is false: `SettingsChannelExitCodeTests` (Unit+Fast) drives the real `ServerSettingsStore` through `ConfigCommands` at argv level; `DelegatingMemoryStore` already exists in Integration/Setup; the production seam is `SqliteMemoryStore`→`LazyServerSettingsStore`. The "tiny adapter" is also unnecessary | plan :78, :186; SettingsChannelExitCodeTests.cs:24-40, :90; PerformanceCommandsRoundTripTests.cs:38; AppRunner.cs:209-236 | Cite the precedents; construct `new LazyServerSettingsStore(_ => Task.FromResult<ISettingsStore>(new ServerSettingsStore(client, Token)))` — the real production class, no invented type; what's novel is only CLI→real-HTTP→endpoint→bank in one process, say that |
| F7 | MINOR | WP3 RED witnesses | "Rename the const" is listed as a RED witness but produces a compile error, not a runtime RED | plan :68; R2 J7/B4 bar | Relabel as compile-time rename protection; witnessed REDs are delete-row / renumber / reword |
| F8 | MINOR | WP3 phrase selection | Plan doesn't pin which phrases; several *obvious* phrases are not contiguous in both texts (19's "differs from this binary's DDL"; 24's "…until it finishes" vs "…until the re-embed finishes") — a mis-chosen phrase vacuous-passes one row | plan :68; configure-ai-raccoon-server.md:370-378; ExitCode.cs:53-54,73-77 | List the per-code phrase and assert both sides in the test (phrase in cell AND phrase in the const's comment) |
| F9 | MINOR | WP1 gate #2, WP4 gate | Risks table says "`--minimum-expected-tests` on every gate" but WP1 gate #2 (`Speed=Fast&Performance!=Benchmark`) and the WP4 `VersionContractTests` gate omit it (R2 N5) | plan :89, :130, :143 | Add the flag (fast lane: loose floor ~3000; VersionContractTests: exact count) |
| F10 | MINOR | WP2 checklist | (a) 12 = 7+4+1 ✓ but R2 §7's live-stuck-bank row and the stale-lease drain case are still unrepresented — state the decision; (b) rows 8-12 need 1.36.1 (exit 25) while the file header says run 1.36.0/pr 591; (c) the WP2 gate checks structure only — command executability is un-gated (draft row 3's command already has a copy-path/data-root mismatch), and the new row's exit-0 leg needs the close-the-row SQL and serve+seed sequence spelled out | plan :96-105; checklist json :2-8, :32 | Update `_note`/`derived` (or split the file); add a reviewer pass over the five new command strings to WP2's AC; spell the `finished_at` UPDATE in the guard row |
| F11 | MINOR | WP1 fixture determinism | Cited seeding precedent uses `:memory:` connections, not a live file bank under a running server — second-connection writes to the served WAL bank need a busy timeout; else `[RetryFact]` masks a genuine lock flake | plan :80; NonDefaultDimensionMigrationTests.cs:124-130, :141-144 | Note the lock-avoidance (short transaction, busy timeout) in the plan; seed idempotently (DELETE+INSERT) as planned |
| F12 | MINOR | WP3 parsing | Table-anchoring under-specified: the doc has other backticked-number tables; also the new Integration class traits are unnamed (copy source = `[Integration][Slow]`, so its exclusion from gate #2 is deliberate) | plan :111-119; configure-ai-raccoon-server.md (other tables); SettingsEndpointTests.cs:24-25 | Anchor the regex to the `| Exit code | Meaning |` header (or the "composes into a script" sentence); declare traits per class |

## Corrected minimum test set (verified gate commands)

| # | test | proves | RED mutation | gate command |
|---|---|---|---|---|
| 1 | `ModelReset_WithAnOpenMigration_IsRefused_Exit25_DeletesNothing` — `[RetryTheory]` over `settings model reset` AND `settings model embedding reset`; seed provider/model + open row (future lease) by raw SQL inside the method; asserts exit 25, stderr == frozen message **verbatim**, all six keys still present via GET | the guard fires on Provider, atomic-by-ordering, 409 body → stderr fidelity, both verbs, delete-nothing | delete the endpoint guard; check-after-delete; map 409→hardcoded message; wire only one verb | `TESTINGPLATFORM_TELEMETRY_OPTOUT=1 dotnet test --project tests/AiRaccoon.Tests --filter "FullyQualifiedName~ModelResetGuardTests|FullyQualifiedName~ModelResetGuardEndpointTests" -v m --minimum-expected-tests 6` (form verified G2; count = 4 integration cases + 1 fast + 1 new pin) |
| 2 | `ModelReset_NoOpenMigration_StillResets_Exit0_ClearsAllSixKeys` — `[RetryTheory]` both verbs; asserts exit 0, six keys gone, success line present | the guard is conditional on `HasOpenModelMigrationAsync`; honest six-key count (anti-swap: any key left behind → RED) | guard fires without an open row; foreach drops a key | same as #1 |
| 3 | `Delete_NonProviderKey_WhileMigrationOpen_StillSucceeds` — direct endpoint DELETE of `embedding.model` → 204 | guard scope is Provider-only (SyncCommands / `model code reset` unaffected) | guard blocks all deletes while open; wrong key compared | same as #1 |
| 4 | `ModelReset_StoreThrowsModelMigrationInProgress_Exits25_WithTheUnprefixedMessage` — Fast, `FakeMemoryStore` override throwing on `DeleteSettingAsync` (literal `25`), via `TestData.CreateConfigCommands` + `CliRun`; asserts err == frozen message, no `ai-raccoon: ai-raccoon:` prefix, stdout lacks the success line | catch inside `ModelResetAsync`: exit mapping + verbatim message + no double prefix (RED today: 15 + doubled prefix — real) | return `InvalidArgument`; `CliFailureFormatting.Format` the message; print the success line too | `TESTINGPLATFORM_TELEMETRY_OPTOUT=1 dotnet test --project tests/AiRaccoon.Tests --filter "FullyQualifiedName~ModelResetGuardTests" -v m --minimum-expected-tests 1` (form verified G2) |
| 5 | `ModelEmbeddingSet_WhileMigrationOpen_StillExitsInvalidArgument` — Fast (NEW, F3); mirrors SettingsChannelExitCodeTests.cs:61-86 with a throwing `IModelMigrationStore` | A2 catch placement — `model embedding set`-while-open keeps today's exit 15 (out-of-scope change stays out) | dispatcher-level catch; catch in `ModelSetLocalAsync` | same as #4 (count 2) |
| 6 | `HowToExitTable_ListsExactlyTheDoctorReachableExitCodes` — Fast; regex the exit table (anchored), set-equality vs the explicit doctor-reachable const list | table add/delete/renumber drift | delete the `24` row; renumber 24→25; add 21 | `TESTINGPLATFORM_TELEMETRY_OPTOUT=1 dotnet test --project tests/AiRaccoon.Tests --filter "FullyQualifiedName~HowToExitTableTests" -v m --minimum-expected-tests 2` (form verified G3) |
| 7 | `HowToExitTable_EachMeaning_MatchesTheConstsDocComment` — Fast; per code: 19/20/22/24 phrase cross-checked against the const's doc comment in `ExitCode.cs` (via `TestData.RepoFile`), 0/1/2 table-pinned literals (F4) | meaning/doc-comment drift | reword row 24's "schema shape is healthy"; reword row 20's "user_version" cell; reword row 1's cell | same as #6 |
| 8 | checklist WP2 — 12 rows, 7 fields, JSON parses + reviewer pass on command strings (F10) | manual legs incl. guard row (both verbs → EXIT=25, close-row SQL → EXIT=0, six keys gone) | n/a — human run post-1.36.1 | `python3 -c "import json;d=json.load(open('docs/work/checklist/2026-08-26-1.36.0-doctor-memory-engine.json'));ids=[i['item'] for i in d['items']];assert len(ids)==12,ids;print(chr(10).join(ids))"` (parses today; asserts 7≠12 → fails loudly until WP2 lands — correct direction) |
| 9 | WP4 — VERSION 1.36.1 + `VersionContractTests` + README + PR title | release mechanics | wrong version | `python3 scripts/version-bump.py patch` (verified G5); `TESTINGPLATFORM_TELEMETRY_OPTOUT=1 dotnet test --project tests/AiRaccoon.Tests --filter "FullyQualifiedName~VersionContractTests" -v m --minimum-expected-tests <exact>` (add the flag, F9) |

