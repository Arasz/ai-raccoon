# G4 — Consumer surface (CLI & agent-facing UX): adversarial verification

Verifier's base: `/Users/arasz/RiderProjects/ai-raccoon` @ `5bca1900` (== the review's base; `git diff HEAD -- docs/ src/` empty). Installed tool under test: `1.42.5+8a1f1dde`. Every live run used `--data-root docs/work/2026-09-22-project-scope-review/verify/g4/...` on explicit ports 7930–7996; port 7721 and `~/.ai-raccoon` were never read, written or dialled; all scratch backends were stopped before finishing (only the owner's `--data-root /Users/arasz/.ai-raccoon` server remains).

### F6 — CONFIRMED
**Method:** `ai-raccoon --data-root …/g4/data/f6 doctor`; MCP stdio calls via `g4/mcp.py` (write + `memory_search` kind=memory/code/both) against scratch backend 7930; read `README.md:132`, `docs/how-to/configure-embedding-engines.md:13,31`, `src/AiRaccoon/Setup/McpServerInstructions.cs:14-23`, `src/AiRaccoon/Tools/MemoryTools.cs:110-116`, `Core/Memory/SearchWarnings.cs`, `Core/Memory/SearchDispatcher.cs:24-41`.
**Result:** `doctor`: `memory engine: not configured — run 'ai-raccoon model embedding set local' …`; `kind=memory` → `participatingLegs:["fts"]` and **no `warning`**; `kind=code` and `kind=both` → `code engine not configured — FTS5-only results; run 'ai-raccoon model code set default' …`. `SearchResults` carries no warning field and the dispatcher copies only `codeWarning`, so no memory-leg warning exists on any path. Caveat: `doctor` *does* print a memory-specific remedy, so "the only warning available" is exact for the MCP search surface the finding names, not for the whole product.

### F42 — CONFIRMED
**Method:** 5 fresh roots: `ai-raccoon --data-root …/g4/data/qN --port 795N --quiet settings retrieval list`; malformed-line count with `grep -cvE '^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9:.+]+ \[[A-Za-z]+\] '`; `ps -eo pid,args`; read `Hosting/Common/BackendLaunchArguments.cs:53`, `Setup/Logging/QuietLogging.cs:30`, `Setup/Logging/QuietFileLoggerProvider.cs:33-34,61`, `docs/reference/agent-memory-server.md:504-510`.
**Result:** 5/5 runs produced 30 log lines of which 3 were malformed — e.g. a bare `verify/g4/data/f42-x/mcp-token` line and `2026-09-22T13:36:42026-09-22T13:36:40.3825550+00:00 [Debug] …`. `ps` shows the spawned backend runs `--quiet serve --port N`; both processes append to one fixed path through `FileShare.ReadWrite` with only an in-process `lock (gate)`. Torn, interleaved writes confirmed; the reference (504-510) does make this file the quiet-mode diagnostic of record.

### F43 — CONFIRMED
**Method:** `docs/reference/agent-memory-server.md:565` at HEAD (`git diff HEAD -- docs` empty); live: scratch server on 7961, then `serve --restart --port 7961` from tokenless root B (exit 11) and from root C holding a foreign token (exit 12); `ExitCode.cs:17-19,23-45`; `grep -rn "Every way the cycle can fail"` hits only the reference.
**Result:** The reference still claims “Every way the cycle can fail exits `8`”; the two live failures returned **11** (`RestartNoToken`, the “holds no token … it may serve another data root” line) and **12** (`RestartTokenRefused`), each naming the port and the manual escape. `ExitCode.cs:17-19` retires 8 (“retired rather than narrowed”) and defines 10/11/12/13/14/16; no restart failure can return 8. Confirmed; 10/13/14/16 were read, not exercised.

### F50 — CONFIRMED
**Method:** the tutorial's exact Step 4 call via `g4/mcp.py` against scratch backend 7963: `memory_search {"projectId":"get-started","query":"install verification"}`; `docs/tutorials/get-started-with-ai-raccoon.md:106`; `README.md:37`; `src/AiRaccoon/Tools/MemoryTools.cs:134,181`.
**Result:** → `invalid-argument: The arguments dictionary is missing a value for the required parameter 'sessionId'. (Parameter 'arguments')`, `isError:true`. `sessionId` is a required parameter (no default) and the 1.38.0 breaking change is recorded at `README.md:37`; the Step 4 text still omits it and its failure advice sends the reader to `.mcp.json`. Counter-argument: an agent that reads the schema may add `sessionId`; the call *as documented* still fails, which is the finding.

### F51 — CONFIRMED
**Method:** scratch root B holding a bank+token, foreign scratch server on explicit free port 7964: `ai-raccoon --data-root …/g4/data/f51-b --port 7964 model embedding set local` → 17; same root on its own 7965 → exit 0 + `embedding engine set to local`; `settings model show` after the refusal still `provider: (none …)`; `--port` after the verb → exit 15; `DefaultOptions.cs:7` / `CliCommandTree.cs:32` default 7721. Never bound or touched 7721.
**Result:** Reproduced verbatim: `the settings server at http://127.0.0.1:7964/mcp refused this credential — it may serve another data root`, exit 17, no activation. `--port` must precede the verb, and no doc pairs `--data-root` with `--port` for settings verbs (`configure-embedding-engines.md:44` is the bare command). Nuance: a *tokenless* root on a foreign port returns 18 with a different message; the activation scenario (a bank that already holds a token) returns exactly 17 as the finding says.

### F52 — CONFIRMED
**Method:** scratch bank with `provider: local`, 4 unrelated entries written via MCP + `memory_embed_pending`, then `memory_search kind=memory` for `quantum chromodynamics lattice gauge` vs `sourdough starter hydration`; `MemoryTools.cs:134-150` (`minRelativeScore` default 0.6).
**Result:** Nonsense query → **4/4** results, top `ranking: 1.0`, `warning: null`, `fusionStats{topMargin: 0.0161, maxPossible: 0.0164, participatingLegs:["vector"]}`, top `fusionStrength: 1.0` and cosines 0.074/0.004/−0.007/−0.012; the genuinely matching query on the same bank measured `topMargin: 0.508`. Counter-argument found and weighed: the `minRelativeScore` description already warns “rank 1 always scores 1.0 even when nothing in the bank answers the query” — but the payload the finding describes is exactly what a caller sees.

### F53 — CORRECTED
**Method:** MCP calls on a default-`rw` scratch bank: `memory_set_ttl`, `memory_sweep{dryRun:false}`, `memory_sweep{dryRun:true}`, `memory_sweep` (omitted), `memory_delete`, `memory_ingest_file "/etc/hosts"`, `memory_watch_add`, `memory_search` without projectId, `memory_promotion_list{allProjects:false}`; `Tools/SweepTools.cs:35` (`dryRun ? AccessRequirement.Read : AccessRequirement.Destructive`), `Access/MemoryAccessGuard.cs`, `Core/Access/AccessModePolicy.cs` (`Resolve` → `… ?? AccessMode.Rw`), `Tools/ToolRefusals.cs:31,36`.
**Result:** The refusal texts reproduce verbatim — `access-denied: memory_set_ttl requires mode full (current rw)`, the same for `memory_sweep` with `dryRun=false` and `memory_delete`; `path-outside-scope: Path '/etc/hosts' is outside the ingest scope.`; `watching-disabled: Watching is disabled for project 'g4-f52'.` — while sibling `invalid-params` refusals name their remedy (`… register this directory with memory_watch_add / settings ingest scope add`, `… pass projectId … or allProjects=true …`).
**Exact replacement for the over-general sentence:** “On a default `rw` bank the destructive lifecycle arms refuse — `memory_set_ttl`, `memory_sweep{dryRun:false}` and `memory_delete` return `access-denied: <tool> requires mode full (current rw)`, as do the ingest-scope and watch refusals — and none names the setting or CLI verb that changes it. The sweep's default (`dryRun=true`, and an omitted `dryRun`) is **permitted** on `rw` (measured: `candidates:[], deleted:[]`, no error) because `SweepTools.cs:35` requires Read for the listing and Destructive only for the delete.” So “the whole TTL/sweep/delete surface refuses” overstates by including the dry-run listing; the finding's headline and the access-denied evidence are otherwise accurate.

### F37 — CONFIRMED
**Method:** read `Setup/Cli/Commands/ConfigCommands.cs:168` (bare `catch (Exception ex)` → `ExitCode.InvalidArgument`) with `BackendLauncher.cs` and `ServerProbe.cs` rethrowing the caller's cancellation, `grep -c "when (ex is not OperationCanceledException)" src/` = 14, no `130` in `src/`; live `g4/f37_repro.py` (TCP listener that never answers on 7970, SIGINT at 2.0 s inside the acquire) and `g4/f37b_repro.py` (SIGINT at 0.25–0.65 s on fresh roots, after the spawn).
**Result:** Both repro sets: `EXITCODE: 15`, empty stdout, stderr `ai-raccoon: The operation was canceled.` (one run `A task was canceled.`), where the 14-catch convention would give 130. The post-spawn runs (5/5) also left the freshly started backend alive and reparented (PIDs 34892/34917/34920/34923/34926) with nothing said about it — the finding's secondary observation holds, and F38 confirms the same orphan shape.

### F38 — CONFIRMED
**Method:** `ai-raccoon --data-root …/g4/data/f38 --port 7971 settings maintenance list` (exit 0), then `ps -eo pid,args`, then a 20 s-later `POST 127.0.0.1:7971/mcp`; read `BackendLaunchArguments.cs:46-59`, `BackendLauncher.cs:12-16`, `DefaultOptions.cs:14`, `NodeRunner.cs:39`, `Watchdog/WatchdogRegistrations.cs`; non-quiet `serve --port 7993` log; `grep -in "idle|lifetime|terminate" docs/adr/0075-only-the-server-writes-to-the-bank.md` = 0 hits.
**Result:** After exit 0 the backend (PID 34268, PPID 1) was alive, still answering 20 s later (`HTTP 401` on `/mcp`), launched as `… serve --port 7971` with **no `--idle-timeout`**. `NodeRunner.cs:39` then falls back to `DefaultOptions.IdleTimeout = 4 h` (a bare `serve --port 7993` logs `idle watchdog armed (04:00:00)`), `BackendLauncher` “never kills, signals or terminates the backend”, and the only disclosure is `ai-raccoon: starting the backend on port 7971`; ADR-0075 discusses auto-start but not lifetime or idle timeout.

## Verdict tally
- CONFIRMED: 8 (F6, F37, F38, F42, F43, F50, F51, F52)
- CORRECTED: 1 (F53)
- REFUTED: 0
- Total: 9

## Still open
- **F51, the literal 7721 scenario:** never bound or dialled 7721 (the owner's live `--data-root /Users/arasz/.ai-raccoon` server owns it). Reproduced on an explicit foreign port (7964) with the identical code path; the 7721 default was verified from `DefaultOptions.Port`/`CliCommandTree.cs:32`, not measured.
- **F43 codes 10, 13, 14, 16:** read from `ExitCode.cs`/`NodeRunner` refusal and bind paths, not exercised (13 needs a pre-`/shutdown` server, 14 a shutdown that never frees the port, 10/16 are bind races). 11 and 12 were measured live.
- **F38's full expiry:** the 4-hour watchdog was not waited out; 4 h is established from the code default plus the `idle watchdog armed (04:00:00)` line of a `serve` invoked exactly as the launcher invokes it (no `--idle-timeout`).
- **Quiet-mode logging gap (side observation, unexplained):** a `--quiet serve` never wrote `idle watchdog armed` to `quiet.log` while a non-quiet `serve` did (twice each). It does not change F38's verdict (the default, non-quiet backend keeps the 4-hour watchdog) but may be its own observability defect.
- **F6 severity (HIGH):** facts re-derived; the HIGH-vs-MEDIUM ranking is a judgement call I did not independently re-score.
- **F37's exact transcript (PID 187, port 7911):** not reproduced byte-for-byte — same symptom at different ports/PIDs, 7 times.
