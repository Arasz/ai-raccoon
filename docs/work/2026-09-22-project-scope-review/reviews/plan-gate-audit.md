# Plan gate audit — `PHASE5-PLAN.md` (pre-implementation)

**Target:** every gate in `docs/work/2026-09-22-project-scope-review/PHASE5-PLAN.md`, against `docs/work/2026-09-22-project-scope-review/REVIEW-ASSEMBLED.md` (72 findings, base `5bca1900`).
**Repo audited:** `/Users/arasz/RiderProjects/ai-raccoon` @ `5bca19000d127547ddbc56e2a464113c844334cc` (working tree clean except two pre-existing persona-pin edits under `.ai-badger/agents/`; neither touched).
**Method:** read-only static verification. For each gate I checked the guarded behaviour in the source, whether the current code/test state is the one the plan calls red, and whether the named seam exists and is reachable (public API, injectable port, real-bank integration harness, process harness, or MCP surface). **No test was executed** (read-only; `dotnet test` writes `bin/obj`), so "red today" combines the plan's cited G1–G5 measurements with my code trace, not an independent re-run. Citations use `rg` because the repo's text-search gate intercepts `grep`; no `mcp_ai-raccoon_*` tool and no `~/.ai-raccoon` access.

Legend — **HONEST:** "YES" means the gate fails on a named mutation of the guarded behaviour and the *current* build is already in the failing state; "NO" means it cannot be seen red as written. **FEASIBLE:** existing seam with `path:line`, or "created" when the test file/API has to be built (with the seam it will drive).

| package | gate | HONEST? | FEASIBLE? | what to change |
|---|---|---|---|---|
| P1.1 | `memory_search kind=memory` on provider-unset bank → non-empty warning naming `model embedding set local`; `kind=code`/`both` keep their warning | **Memory half YES** — remove the new composition at `src/AiRaccoon/Tools/MemoryTools.cs:231` and the warning is `null`; the current build is red and pins that state in `Search_KindMemory_HasNoEngineNotConfiguredWarning` (`tests/AiRaccoon.Tests/Unit/Mcp/MemorySearchKindToolTests.cs:272`). **"Keep" half is a regression control, not a watched red** (passes today; only fails if the fix drops the code warning). | Yes — warning wire field `SearchDispatchResult.cs:11-15`, tool compose at `MemoryTools.cs:231`; real-bank seam already exists: `Integration/Mcp/MemorySearchCodeIntegrationTests.cs:57` uses a real bank + `SettingsOnlyStore`. | Name the test file and state the provider-unset fixture is real (not a stub). **Update/retire `Search_KindMemory_HasNoEngineNotConfiguredWarning` (`MemorySearchKindToolTests.cs:272`) in the same PR** or the fix turns an existing green red — the plan doesn't mention this collision. |
| P1.2(a) | shared write → propose → queue row survives with `reasons:["agent-requested-share"]` | **YES, only against the real queue** — write stamps `ScorerVersion = 0` (`Core/Memory/PromotionQueue.cs:15`; `MemoryWriteService.cs:57-65` passes no version) and `ClearStale` deletes `scorer_version != 2` (`Infrastructure/Sqlite/PromotionQueueSql.cs:124-127`, `PromotionScorer.cs:16`). Through the existing `FakePromotionQueue` (`Integration/Memory/SharedWriteIsAPromotionRequestTests.cs:25-32`) the test passes today — a vacuous variant. | Yes — real `SqlitePromotionQueueStore` + runner + writer seams shown in `Integration/Storage/PromotionQueueDiscardTests.cs:24` and `Integration/Storage/SqlitePromotionQueueStoreTests.cs:20`. | State that the test drives the real queue and reads `promotion_queue` (`count(*)`, `reasons`); ban the fake in this gate's wording. |
| P1.2(b) | after `memory_promotion_discard`, identical rewrite does not report `queued-for-promotion` | **YES, only against the real queue** — `MemoryWriteService.cs:65` sets the reason unconditionally while `Upsert` is refused by `promotion_discards` (`PromotionQueueSql.cs:9-13`). Fake queue cannot reproduce the refusal. | Yes — same real-queue harness as (a). | Assert both the response reason and `promotion_queue` count = 0; use `SqlitePromotionQueueStore` + `MemoryWriteService`. |
| P1.3 | squatter answers `/mcp` with valid JSON-RPC but `serverInfo.name = "not-ai-raccoon"` → proxy refuses and does not transmit token | **YES for the proxy/attach arm** — `ServerProbe.cs:69` accepts any body containing `"jsonrpc"`, `BackendLauncher.cs:52-56` attaches, and `BackendSessions.OpenAsync` reads the token after acquire (`BackendSessions.cs:29-40`). **Vacuous arm:** `serve --restart` already refuses a foreign name (`ServerRestart.cs:70`; test `Integration/Setup/Serve/ServeRestartTests.cs:156`), so a gate written against restart passes on the current build. **Missing positive control:** "refuse every listener" satisfies the gate as written. | Yes — handler-injection seam `Unit/Hosting/ServerProbeVerdictTests.cs:77`; real `TcpListener` seam `Integration/Setup/Serve/ServerProbeTests.cs:51`; `BackendSessionsTests.cs:19`; proxy E2E `E2E/ProxyForwardTests.cs:25`, `ProxySpawnedBackendE2ETests.cs:22`. Note `server/discover` appears nowhere under `src/` today, so the fix introduces the call. | Target the proxy/launcher path (not restart) and add the positive control: a real ai-raccoon listener still receives the token / opens a session. Owner decision 4 still governs whether name-based identity is the accepted bar. |
| P2.1(a) | `memory_delete_context` → sync → row stays deleted | **YES** — `DeleteContextAsync` is a bare `DELETE` with no tombstone (`SqliteMemoryStore.cs:302-321`); the merge re-inserts (`SyncService.cs:440-455`); G1 measured the row returning. | Yes — real store + `SyncService` + `FakeCloudStore` (`src/AiRaccoon.Infrastructure/Sync/FakeCloudStore.cs:7`; harness style `Integration/Sync/SyncServiceTests.cs:15`; `TestData.CreateMemoryStore`). | Name the file (new integration test or `SyncServiceTests`); assert the tombstone set is written inside the delete transaction. |
| P2.1(b) | workspace+committed twin, `memory_delete` → sync → both stay deleted | **ORDER-DEPENDENT — not a reliable gate as written.** `DeleteCoreAsync` probes one row with `QueryFirstOrDefault` then deletes all matches (`SqliteMemoryStore.cs:744-785`). Workspace row first (scope NULL) → zero tombstones → committed twin returns (the G1 red). Committed row first → one `project` tombstone → the test can pass on the broken build (workspace rows are stripped from the payload). The plan does not pin the fixture order. | Yes — same harness as (a); workspace rows already exercised in `Integration/Isolation/WorkspaceWriteDedupTests.cs`. | Either force the measured order (workspace row inserted first) or, better, assert both `(project, hash, 'project')` and `(project, hash, 'workspace')` tombstones exist after the delete, independent of probe order. |
| P2.1(c) | two-replica: two committed rows with different scopes, sync → neither returns | **YES** — at most one tombstone is written for the probe's scope, and the merge suppresses per scope (`SyncService.cs:518-524`), so the unrecorded scope returns; red is ordering-independent. | Yes — same harness; `FakeCloudStore` gives the two-replica round trip. | Nothing structural; keep the assertion "both scopes gone", which is exactly what the single-probe bug cannot satisfy. |
| P2.1(d) | watcher deletes a file → sync → chunks stay deleted | **YES** — `DeleteSourcePathAsync` writes no tombstones (`SqliteMemoryStore.cs:329-368`); G1 upgraded this to measured. | Yes — `DeleteSourcePathAsync` is public and is the watcher's delete path (`WatchDigestExecutor.cs:138`); `Integration/WatchIntegrationTests.cs` exists for the watcher. | Drive the digest/delete path rather than a real `FileSystemWatcher` event to avoid timing flake; name the file. |
| P2.2 | re-create deleted content → sync → content survives | **YES** — merge-applied tombstones delete by `(hash, scope, project)` with no `created_at` guard (`SyncService.cs:518-524`); G1 measured the re-added fact deleted. | Yes — `SyncServiceTests` harness; the existing `MemorySync_TombstonePropagation_NoResurrection` (`SyncServiceTests.cs:235`) never re-creates the hash, so the new test fills a real gap. | Add the re-create step explicitly; if owner decision 2 chooses "delete wins forever", the gate must become "re-write under a new hash/path and the tool docs say the old hash is poisoned". |
| F38 | run any settings verb → assert no backend survives the command | **YES today** — no `--idle-timeout` is passed (`Hosting/Common/BackendLaunchArguments` builds `serve` without it; `Setup/DefaultOptions.cs` 4 h); G4 measured a live backend 20 s after exit 0. **Conditional on owner decision 3:** if the chosen fix is a short idle timeout, "no backend survives" needs a bounded wait, not immediate absence. | Yes — real CLI process harness `TestHelpers/RaccoonProcess.cs:23`; pattern `Integration/Setup/CliBankWriteTests.cs:203`; port probe + cleanup seam `TestHelpers/RaccoonBackendCleanup.cs:14`. `RaccoonProcess.RunAsync`'s `finally KillTree` only kills a still-running CLI, so the orphan is observable (don't let fixture `Dispose` clean up before the assertion). | State the assertion point (before `RaccoonBackendCleanup`) and make it match the decision: immediate gone vs gone within the short timeout. |
| F37 | SIGINT during auto-start acquire → exit 130 + message that the command did nothing | **YES behaviourally** — the bare `catch (Exception ex)` at `Setup/Cli/Commands/ConfigCommands.cs:168` returns `InvalidArgument` (15) for `OperationCanceledException`; measured 15. **But the stated procedure is a timing window**, and exit 130 is still an open owner question (assembled record, Lane E Q2), not in the plan's own owner-decision list. | Partly — `Unit/Setup/Serve/AppRunnerShutdownCancellationTests.cs:23` invokes the signal handler directly (no real signal); `RaccoonProcess` has no signal sender, so a real-SIGINT test needs new code and Unix gating. The deterministic alternative is injecting cancellation (token or fake launcher) into `ConfigCommands.Run`. | Pin the exit-code decision; write the gate deterministically (pre-cancelled/injected token or direct handler invocation) and assert the "nothing changed" message; if a real-SIGINT test is required, synchronize on the `starting the backend` stderr line and platform-gate it. |
| F26 | multi-chunk write → `memory_delete(returnedHash)` → write fully gone, or response advertised N | **YES today** — `WriteResult` carries one hash (`Tools/MemoryTools.cs:465`), the store writes N chunks but returns `chunks[0]`, delete is hash-scoped; measured 1 of N. | Yes — `WriteResult` is the contract; real-store integration seams (`TestData.CreateMemoryStore`) and tool tests exist (`Unit/Mcp/MemoryToolsTests.cs:30`). | The disjunction weakens the acceptance: "response advertised N" leaves the rest of the write undeletable (the finding's cost). If the defect is "rest stays searchable", require delete-by-path or full hash set, not just N. |
| F52 | nonsense query on ≥4-entry bank → response empty or explicitly marked low-confidence | **YES today** — no absolute floor; measured 4/4 at `ranking: 1.0`. **Missing positive control:** a fix that warns on every query passes. "Low-confidence marker" is not a wire contract anywhere today — the fix must define it. | Yes — real-bank MCP harness `Integration/GoldenMemorySearchResponseTests.cs:48`, `Integration/SearchSignalPreservationStageOneTests.cs:40`; `fusionStats` already on the wire. | Define the marker's name/shape in the plan or a doc row; add the positive control (genuine match returns unmarked results) so "warn always" cannot satisfy the gate. |
| F72 | refused canary with OTLP attached → canary absent from log **and** span | **YES today** — `MemoryTools.cs:208` embeds `QuerySnippet` in the refusal; `ToolRefusals.cs:244` logs it (EventId 910); span status/exception set in `Observability/ToolExecutionActivity.cs`; G3 captured both. **Negative-only gate:** logging nothing satisfies absence. | Yes — `FakeLogger` is available (`Microsoft.Extensions.Diagnostics.Testing` in `AiRaccoon.Tests.csproj:15`); span capture via `ActivityListener` (`Unit/Observability/MemoryToolsInstrumentationTests.cs:39`). | Add the positive half: the new diagnostic carries policy name + query length/hash, so suppression alone cannot pass. |
| F53 | access/watch/scope refusals name the remedy | **YES today** — measured message has tool + modes only (`Core/Access/AccessModePolicy.cs:26`, `Access/MemoryAccessGuard.cs`). | Yes — `Unit/Mcp/MemoryToolsAccessModeTests.cs:27`, `Integration/Mcp/ToolRefusalsTests.cs`. | Pin the exact remedy string and assert all three refusal families named in the change (access-denied, path-outside-scope, watching-disabled), not just one. |
| F24 | write with both `context` and `workspace_id` → row lands in the outbox | **YES today** — `ContextResolver` returns `Context` whenever non-empty (`Unit/Search/ContextResolverTests.cs` covers the resolver); measured row committed project-wide. **Contradiction in the plan:** the Change allows "refuse **or** make `workspace_id` win", but the Gate only asserts the row lands in the outbox — a refusal does not land it there, so the refuse arm is silently excluded. | Yes — workspace integration seams `Integration/Isolation/WorkspaceIsolationTests.cs`, `WorkspaceWriteDedupTests.cs`, plus the resolver unit tests. | Fix the gate to match the chosen mutex (outbox assertion for "workspace wins"; refusal assertion for "refuse"), and align the two tool descriptions. |
| F7(a) | corrupt bank → exit 2 / documented code + clean message | **YES today** — `DoctorCommands.cs:41-51` wraps only `OpenBankReadOnlyAsync`; Microsoft.Data.Sqlite defers the error to `SchemaDoctor.DiagnoseAsync`, so it escapes to `ConfigCommands.cs:168` as exit 15 + raw SQLite text; measured. | Yes — `Unit/Setup/DoctorCommandsTests.cs:29` drives real argv through `CliRun`/`ConfigCommands`; `Unit/Setup/Diagnostics/HowToExitTableTests.cs:18` pins the documented table. | Pin the exact code (`FailedToOpenEncryptedBank` = 2) instead of "or the documented code", and assert the message is one line with no SQLite/parameter text. |
| F7(b) | millisecond `started_at` → no raw .NET parameter text | **YES today** — unguarded `DateTimeOffset.FromUnixTimeSeconds` (`DoctorCommands.cs:317-319`); measured raw `Parameter 'seconds'` text. **Negative-only:** printing nothing passes. | Yes — seed `model_migration.started_at` via SQL as `DoctorCommandsTests.cs:768` does, run doctor via `CliRun`. | Add the positive assertion (timestamp rendered as a date or `model migration: unreadable`), matching the "format defensively" arm. |
| F65 | regex test over synthetic diffs incl. both controls | **YES today** (extract regex) — `build.yml:75` lacks `(^|/)global\.json$` and `^benchmarks/`; both classify `code=false`. | Created — no workflow-regex test exists. The test must read `CODE_REGEX` **out of `build.yml`**, not duplicate it, or drift passes; and it must be added to the CI pytest list (`build.yml:275`) or it never runs. | Parse the workflow regex; controls must include a matched positive (`src/x.cs` true), a negative (`docs/x.md` false), and the two fixed paths. |
| F66 | every `scripts/tests/*.py` either in the CI list or an explicit exclusion map | **YES today** — 51 files, 14 in `build.yml:275`, 33 unnamed. **Vacuous risk:** an unbounded exclusion map makes the gate trivially green. | Created — new python test; it must itself be added to the CI list in the same change (circular wiring), or F65/F66 tests stay unrun. | Cap the exclusion map (the two `chromadb` collection-error files) or require a reason per entry; wire the test into `build.yml` as part of the fix. |
| F67 | crash a console app under `DOTNET_Dbg*` in a job without `dumps/` → no dump | **Not a durable gate** — a one-off CI observation whose red depends on the CI job's environment (`build.yml:99-103` sets the vars; `build-slow` has `mkdir -p dumps` at `:220`, `build-fast` does not). No test exists for dump *creation* (`scripts/tests/test_coredump.py` tests the triage parser only). | No existing seam; the minimal automated form is a workflow-structure assertion (the `WorkflowActionPinTests.cs:15` YAML-parsing pattern): `mkdir -p dumps` precedes the test step and an `upload-artifact` runs on `failure()`. | Replace the crash observation with a YAML structure gate, or add a deliberate crash-probe step to `build-fast` that fails the job when no dump lands. |
| F63 | dry-run reconciliation against the 21 known-missing versions | **Not a real gate as written** — no reconciliation artifact exists; the red depends on live nuget.org state and a historical list that changes; it would assert on the fix's own script. | No — new script + network; `publish.yml`/`release.yml` are separate and manually dispatched. | Implement the checker with an offline fixture mode and unit-test that (fixture with a missing version → nonzero; present version → zero); wire it into `release.yml`. The watched red is the fixture, not the live 21. |
| F64(a) | flake ledger tolerates its entry under the new runner | **Not red on the current build** — `scripts/tests/test_nightly_triage.py:80` already passes the ledger classification; what is missing is the runner. | Created — the gate must assert new wiring; YAML-parsing seam `Unit/WorkflowActionPinTests.cs:15`; artifacts `scripts/nightly-triage.py:41`, `known-flakes.json`. | Gate on "a scheduled job exists and invokes `nightly-triage.py` with the ledger" (fails today: no `schedule:` anywhere in `.github/workflows/`), then a dry run over the ledger entry. |
| F64(b) | retry-recovered failure emits a diagnostic | **Red today (F8: no retry trace), but no seam exists to assert it** — retries are external `xRetry.v3` attributes (`tests/AiRaccoon.Tests/AiRaccoon.Tests.csproj:27`); `Unit/TestInfra/RetryFactRetriesOnTransientFailureTests.cs:17` proves only that the retry happens. | No — the fix must create an observable hook (custom attribute/runner, log sink, or captured runner output) before any test can drive it. | Name the diagnostic mechanism and its seam in the plan; otherwise the gate cannot be written against today's code. |
| F4 | guard fails on the old spelling | **YES today** — `PATHS` (`scripts/verify-history-scrubbed.py:20-24`) lacks the two historical spellings, so a fixture repo committing `unit/retrieval/assets/reference-topk.json` passes clean; the existing test builds local fixture repos (`scripts/tests/test_verify_history_scrubbed.py`). | Yes — add fixture cases; but the test file is not in the CI list (`F66`), so the gate needs the CI wiring too. | Add both spellings to `PATHS` and to the test fixtures; wire into CI; note the red is repo-history-dependent by design until #414 lands. |
| F9 | caps must fail on current headroom — "lower to 1050/27 and watch it still pass, then re-derive"; add caps for `MemorySchema.cs`/`SyncService.cs` | **NO — self-contradictory and cannot be seen red.** The file is 1046 against a 1050 cap, so the named procedure asserts a pass. Nothing in the gate describes a failing state; per its own premise ("a gate that cannot be seen red is not a gate"), it is not a gate. | Yes — `Unit/Storage/SqliteMemoryStoreSizeRatchetTests.cs:80-81` exists; new cap facts can be added in the same file. | Set caps to today's measured 1046/26, prove each load-bearing by lowering one below the measurement (1045 / 25) and watching red, then restore. Add `MemorySchema.cs`/`SyncService.cs` cap facts set to today's counts and watch each red at count−1. |
| F10 | unit test injecting a fixture map without touching the static | **Compile-red only** — the parameterized seam does not exist, so the test cannot be written against today's API; `ReplaceDefault` (`Core/Projects/ProjectIdAliasMap.cs:33-60`) is the only substitution route. A compile failure is a red a compiler sees, not a behaviour a mutation can falsify. | Yes once the API lands — helpers are public (`Core/Access/AccessModePolicy.cs:15`, `Core/Watch/WatchConfigKeys.cs:15,19`, `Core/Ingestion/IngestScopeKeys.cs:24`), `ToolGate` is constructor-injected (`Tools/ToolGate.cs:23`). The collection gate `Unit/Projects/ProjectIdAliasDefaultCollectionGateTests.cs:25` must be revisited if call sites change. | Declare the red as compile-time, and add the runtime half: with `Default` holding a deliberately *conflicting* map, the injected fixture map decides the key. |
| Docs sweep | "each doc claim asserted by the existing exit-table/`ToolInventoryTests` pattern" | **PARTIAL / mislabelled.** The existing patterns cover the how-to doctor table (`HowToExitTableTests`), the tool table (`ToolInventoryTests`) and the error-shape table (`ToolRefusalsTests`). They do **not** cover `agent-memory-server.md`'s `serve --restart` exit-8 paragraph (F43), the tutorial Step 4 `sessionId` (F50), the CLI refusal remedy (F51), or `quiet.log` single-writer (F42 — a code fix, not a doc claim). F61/F62/F71 are not doc claims at all: F62 is a fake dropping a parameter, F71 is a ratified sync acceptance. | F43/F50 can be added as doc-assertion tests (tests read repo files via `TestData.RepoFile` and parse markdown); F42 needs a two-process log test that does not exist (`Integration/Setup/QuietLoggingTests.cs` is single-process); F51 is a behaviour assertion, not a doc-table one. | Split the row into per-item gates naming the assertion file for each; drop or relabel F61/F62/F71 so the gate statement matches what is actually asserted. |

## Blockers

Gates that are not real gates, with the minimal change that makes each one:

1. **F9 — self-contradictory watched red.** "Lower to 1050/27 and watch it still pass" asserts a pass on a 1046-line file; there is no failing state and no mutation. *Minimal change:* set the caps to 1046/26, lower to 1045/25 to observe red, restore; add the two new cap facts and observe each red at (measured−1).
2. **P2.1(b) — order-dependent red.** The gate's "both stay deleted" passes on the broken build when the committed row is probed first (one tombstone is written); the G1 red only reproduces with the workspace row first. *Minimal change:* assert the tombstone set covers both scopes after the delete, or fix the seeding order and say so.
3. **F67 — one-off CI observation, no repeatable test.** The gate's red depends on the `build-fast` job environment, and no dump-creation test seam exists. *Minimal change:* assert the workflow structure (`mkdir -p dumps` before the test step + `upload-artifact` on `failure()`) with the `WorkflowActionPinTests` YAML pattern, or add a crash-probe CI step that fails without a dump.
4. **F63 — asserts on a not-yet-existing artifact against live network/historical state.** *Minimal change:* build the checker with an offline fixture mode; unit-test fixture-missing → nonzero and fixture-present → zero; wire it into `release.yml`.
5. **F64(a) — not red today.** The ledger classification already passes; only the missing runner is new. *Minimal change:* gate on the schedule + `nightly-triage.py` invocation in workflow YAML (red today), plus the ledger dry run.
6. **F64(b) — no seam exists.** Retries are external `xRetry.v3` attributes and emit nothing a test can observe. *Minimal change:* define the diagnostic hook (custom retry attribute/runner or log sink) as part of the fix; write the gate against that hook.
7. **Docs sweep — vague and partly mislabelled.** "Each doc claim" has no file, no parser and no list; four of the named items are not doc claims or have no existing pattern. *Minimal change:* one row per item with its assertion file (F43 → a test parsing `agent-memory-server.md`'s restart paragraph; F50 → a test parsing the tutorial's Step 4 call for `sessionId`; F42 → a new two-process `quiet.log` test; drop F61/F62/F71 from this gate).

## Still open

- **`F37` exit code is an unmade owner decision.** The assembled record records Lane E's question "is exit 130 the wanted interrupt code?"; the plan pins 130 and the plan's own owner list does not include it. The gate is honest on the current failure (15) but its target value is not yet ratified.
- **`F38` and `F63` gates are conditional on open owner decisions 3 and 6.** A short idle timeout changes what "no backend survives" means; "tagged-but-unpublished is acceptable" would collapse F63's gate entirely.
- **Undefined wire contracts behind three gates.** F52's "explicitly marked low-confidence" and F72's replacement diagnostic have no named field/shape today; until defined, each gate can be satisfied by a marker the author chooses.
- **No independent re-execution.** I did not run the suite or the CLI (read-only; avoids `bin/obj` writes), so the G1–G5 "watched red" measurements are taken from the plan/record and corroborated only by code tracing. The one red I found to be *not* robust by inspection is P2.1(b) (ordering).
- **F61's `--nologo` hazard applies to the audit environment of the gates themselves.** `dotnet test --no-build --nologo` discovers 0 tests and exits 5 on this repo's MTP path (`docs/plans/…` commands carry it). If any new gate is documented with that switch, it is vacuous; the plan should require commands without `--nologo` for every test invocation.
- **P1.1 golden-response risk is unverified in one direction.** The golden bank configures an engine (`Integration/GoldenMemorySearchResponseTests.cs:48-70`), so warning-string churn may not hit that file; which committed goldens carry warnings was not audited.
- **F10's red is compile-time, not behavioural.** It is acceptable as a shape gate but should be labelled; otherwise it will be cited later as a behavioural watched-red that never existed.
- **F53's remedy string and F24's mutex choice** are still asked as owner questions in the assembled record (Lane G, Lane C Q3); the plan's gates assert specific outcomes ahead of those answers.

