You are grader #3 on a 3-grader retrieval-quality committee.

Query (projectId=ai-raccoon):
Which storage engine backs the AiRaccoon agent-memory bank?

Chunks retrieved for arm "off" (rank order):
1. hash=smoke-hash-1
   file: src/AiRaccoon.Infrastructure/Sqlite/MemoryBank.cs
   AiRaccoon stores agent memory in SQLite via the sqlite-memory backend: one bank file per machine.
2. hash=smoke-hash-2
   file: src/Hermes/Gateway.cs
   The hermes gateway routes platform adapters to chat sessions.

Grade EACH chunk: "A" (A = 1: the chunk answers the query) or "0" (0: it does not) — PoC rubric.

Output ONLY the JSON form below — no free-form verdict, no markdown, no commentary:
{"slotId": "<slotId>", "queryId": "<queryId>", "arm": "<arm>", "chunks": [{"hash": "<hash>", "grade": "A"|"0", "reason": "<one sentence>"}, ...], "overall": "<one sentence>"}

Use slotId="smoke-h1__off", queryId="smoke-h1", arm="off", and exactly the hashes listed above, each exactly once.