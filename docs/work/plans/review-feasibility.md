I have everything I need. Compiling findings.

---

# FINDINGS — `air-backend-launch-identity-proof` feasibility claims

Read-only verification against the working tree (post-#653/#654). No files under `docs/work/plans/` were opened. All line numbers from the current tree.

---

## (1) The 10 verdicts

**1. KEY ALGORITHM — VERIFIED**
- TFM is **net10.0** (`src/AiRaccoon/AiRaccoon.csproj:4`, `AiRaccoon.Core.csproj`, `AiRaccoon.Infrastructure.csproj`, `tests/AiRaccoon.Tests/AiRaccoon.Tests.csproj:4`). `Directory.Build.props:4` sets only `LangVersion=latest` (no TFM).
- **Ed25519 does NOT exist in this BCL.** A sweep of every `System.Security.Cryptography*.dll` ref pack (`/usr/local/share/dotnet/packs/Microsoft.NETCore.App.Ref/10.0.12/ref/net10.0/`) finds **no** `Ed25519`/`EdDSA`/`EdDsa`/`Ed25519ph`/`Ed448` type. The only "Ed25519" strings are the composite post-quantum hybrid names `MLDsa44WithEd25519`/`MLDsa65WithEd25519` in `System.Security.Cryptography.dll` (not Ed25519 signing). The repo itself hand-decodes ed25519 OpenSSH keys (`AiRaccoon.Core/Encryption/OpenSshPrivateKeyParser.cs:7,14`) and hand-derives via HKDF (`SshKeyDerivation.cs:8`) precisely because the BCL has no Ed25519 — corroborating.
- **Existing `ECDsa`/`ECDsaCng` usage: NONE** anywhere in `src/` or `tests/` (grep for `ecdsa|ECDsaCng|nistP256|CreateSigning` returns only `System.Security.Cryptography` usings + the ed25519 parser). Greenfield.
- **P-256 sign+verify is usable with no new package:** `ECDsa`, `ECCurve`, `ECCurveType`, `NamedCurves`, `ECParameters` are all in the in-box `System.Security.Cryptography.Algorithms.dll`; `nistP256` is under `ECCurve.NamedCurves`. The runtime `System.Security.Cryptography.dll` carries `SignData`. `ECDsa.Create(ECCurve.NamedCurves.nistP256)` + `SignData`/`VerifyData` is the in-box path. (My `strings` method-name probe on ref assemblies was unreliable — 0 hits even for `Create` — so I confirmed the type surface + runtime `SignData`; these ECDsa members are standard since .NET Core 3.0 and present in net10.0.)

**2. PROOF ENDPOINT PLACEMENT — VERIFIED (seam named)**
- `src/AiRaccoon/Setup/McpServerSetup.cs:83` `UseMiddleware<McpTokenGate>(gated, new McpTokenFile(config.Options.DataRoot).Path)` runs before endpoint execution; endpoints are minimal-API `Map*`: `MapMcp("/mcp")` (:91), `MapObservability()` (:92), and token-only `MapShutdown()`/`MapSettings()`/… (:95–106) inside `if (gated is not null)`. Loopback listener: `McpServerSetup.cs:67` `Listen(IPAddress.Loopback, serverConfig.Port)`.
- The gate is **default-closed with an allowlist**: `McpTokenGate.cs:31` `OpenPaths = [ObservabilityEndpoint.Path]`; `McpTokenGate.cs:78` `IsGuarded(path) => !OpenPaths.Any(open => path.StartsWithSegments(open))`.
- **Exact seam (middleware order + endpoint registration):** add the proof route's path to `OpenPaths` (`McpTokenGate.cs:31`) so `IsGuarded` returns false → the gate calls `_next` and never demands the token (`McpTokenGate.cs:60-63`); register the route with `MapGet`/`MapPost` in `ConfigureMcpEndpoints` (`McpServerSetup.cs:74-107`), same style as `MapObservability`/`MapShutdown`, on the same loopback listener. Everything outside `OpenPaths` (`/mcp`, `/shutdown`, `/settings`, …) stays gated — **the gate is not weakened**; it is one more allowlisted path (and the allowlist is documented as deliberately under-inclusive so a forgotten entry costs a 401, `McpTokenGate.cs:27-30`).

**3. SERVER PROBE HONESTY — VERIFIED (supports the amended claim)**
- `ServerProbe.cs` builds exactly one request (`ProbeAsync`, ≈`:52-56`): `POST {endpoint}` where `endpoint = /mcp` (`:118` `EndpointFor`), **body = the literal `"x"`** (`new StringContent("x", …, application/json)`), headers **`Content-Type: application/json`** and **`Accept: application/json, text/event-stream`** (`:54-55`).
- **No token is ever sent** — no `X-AiRaccoon-Token`, no `Authorization`. So today's probe sends **zero secret bytes** to an unknown listener.
- Verdict: the probe **supports** the amended claim's "**zero secret bytes**" half already (nothing needs to change for that half). It **does not contradict** "nonce-only challenge" — it simply isn't a nonce challenge yet (fixed `"x"`, no signing). Note the amended wording is a **correct fix**: the old "**zero requests** to a squatter" was false (the probe does send one POST to any listener, squatter included); "zero secret bytes" is what is actually true.

**4. TOKEN READERS/WRITERS — VERIFIED (with the override answer)**
- `src/AiRaccoon/Hosting/Common/McpTokenFile.cs`: `FileName = "mcp-token"` (:15); data-root resolution `Path = Path.Combine(dataRoot, FileName)` (:41); permission code `options.UnixCreateMode = UserRead|UserWrite` (**0600**, POSIX-only) at **:170** (the "~:159-160" in the claim is close — it is :170); writer `TryMintAsync` (:158-176) + `EnsureAsync`/heal (:50-77); reader `Read()` (:138). Directory creation `Directory.CreateDirectory(GetDirectoryName(Path))` (:51).
- **Mint (writer), one place only:** `NodeRunner.cs:42` `new McpTokenFile(...DataRoot)` → `:53` `tokenFile.EnsureAsync` (i.e. only `serve` mints).
- **Readers:** `BackendSessions.cs:45` (field) + `:70`/`:112` `.Read()`; `CliSettingsBackend.cs:72` + `:73` `.Read()`; `ServerRestart.cs:86` `tokenFile.Read()` (token passed in); `McpServerSetup.cs:83` constructs `.Path` for the 401 message only. `McpTokenGate` receives the token **value** via `config.McpToken` and the **path** for messages — it does not read the file. Test helpers: `TestHelpers/RaccoonBackendCleanup.cs:17` `.Read()`; `McpTokenFileTests` and many E2E/serve tests construct/read at arbitrary roots (`_dataRoot`, `nested`, `elsewhere`, `serve.DataRoot`).
- **Env-var/settings override of the path: NONE.** The path is always `<DataRoot>/mcp-token`. The only knob is `--data-root` (`CliArgs.cs:188` → `DefaultOptions.DataRoot` = `~/.ai-raccoon`, `DefaultOptions.cs:13`), expanded by `CliOptionsExtensions.cs:10-26` (`ExpandedDataRoot`). `${AIRACCOON_MCP_TOKEN}` (`McpEntryRenderer.cs:12,15`) is a **client-side header placeholder**, not a path override.
- **What breaks if the path moves to the bank state dir:** the `McpTokenFile(string dataRoot)` ctor contract (:38-41) is the chokepoint — it takes a bare `dataRoot` **string** and knows nothing of `InstallScope`. Relocation must make resolution scope-aware (User → dataRoot; Project → dataRoot/.ai-raccoon), which means either changing the ctor to take `InfrastructureOptions` or passing the resolved bank dir at the **4 production call sites** that today pass `config.Options.DataRoot` (`NodeRunner.cs:42`, `BackendSessions.cs:45`, `CliSettingsBackend.cs:72`, `McpServerSetup.cs:83`). Most **User-scope** tests are unaffected (User maps to dataRoot, so `<dataRoot>/mcp-token` is unchanged); only Project-scope path expectations and literal-path assertions move. Wording in the `McpTokenFile` header ("under the data root") and `McpTokenGate`'s 401 text is cosmetic. The 0600 `UnixCreateMode` is directory-agnostic and `EnsureAsync` already creates the directory.

**5. RELOCATION TARGET — VERIFIED (with a reachability correction)**
- `src/AiRaccoon.Infrastructure/Sqlite/SqliteConnectionFactory.cs:166-174` `BankDirectoryFor(options)`: `User => options.DataRoot`, **`Project => Path.Combine(options.DataRoot, ".ai-raccoon")` (:170)** — matches the claim's `<dataRoot>/.ai-raccoon`. `BankPathFor(options) = BankDirectoryFor + "memory.db"` at **:176**.
- **Reachable from the token/key mint code: YES.** `NodeRunner.cs` (which mints the token at :42) already calls `SqliteConnectionFactory.BankPathFor(descriptor.LaunchConfig.Options)` at **:277**. `ServerConfig.Options` is `AiRaccoon.Infrastructure.Options.InfrastructureOptions` (`ServerConfig.cs:13`), carrying `DataRoot` + `Scope`.
- **Call path a relocation would use (settings type → path):** `ServerConfig.Options` (`InfrastructureOptions`) → `SqliteConnectionFactory.BankPathFor(options)` (**public**, :176) → `Path.GetDirectoryName(...)` = the bank state dir. This idiom already exists in the codebase: `MaintenanceCommands.cs:19` and `QuietLogging.cs:30` both do `Path.Combine(Path.GetDirectoryName(SqliteConnectionFactory.BankPathFor(options))!, …)`.

**6. F39 NOBANK — VERIFIED**
- `ExitCode.NoBank = 22` (`src/AiRaccoon/ExitCode.cs:66`; doc comment :64-65). Confirmed by `tests/Unit/ExitCodeTests.cs:22` `NoBank.ShouldBe(22)`.
- **Consumers / current producer:** only `DoctorCommands.cs:26` `return ExitCode.NoBank` produces it. Also referenced at `ExitCode.cs:87` (doc), `tests/Unit/Setup/DoctorCommandsTests.cs:58`, `tests/Unit/Setup/Diagnostics/HowToExitTableTests.cs:30`.
- **`CliWriteOptOuts.cs:16`** `WritesDirectly(...) => commandPath is ["encryption", ..]` — the sole direct-write exception. Its twin in the same file, `BankCapableCliCommandAllowlist` (`CliWriteOptOuts.cs:23+`), names the only three bank-opening commands (`EncryptionCommands`, `DoctorCommands`, `ServeCommands`), enforced by the reflection test `CliCommandsDoNotOpenTheBankTests` (derives every CLI command's object graph and fails if it resolves a live `ISqliteConnectionFactory`). This is the existing F39 enforcement seam.
- **Two auto-start composition roots:** (a) **ProxyRunner** (`Hosting/Proxy/ProxyRunner.cs:11`) → `BackendSessions.AcquireBackend` (`BackendSessions.cs:174-185`); (b) **Settings/ConfigCommands** (`Setup/Cli/Commands/ConfigCommands.cs:141-142`) → `CliSettingsBackend.AcquireAsync` (`CliSettingsBackend.cs:43-57`). Both funnel to `backendLauncher.StartPrivateAsync`/`AcquireAsync` (`BackendSessions.cs:184-185`, `CliSettingsBackend.cs:57`).
- **What spawns the child:** `BackendLauncher.Start` → `ProcessStartInfo` + `Process.Start` (`Hosting/Proxy/BackendLauncher.cs:187-202`), shared by both roots. The child is `serve` (`BackendLaunchArguments.cs:67` `["serve","--port",…]`).
- **Child opens the bank:** `NodeRunner.cs:114` `connectionFactory.ProbeUsingEncryptionKey(...)` and `:126` `connectionFactory.OpenBankAsync(...)` — this is where a missing bank would be minted/opened.
- **Bank-missing detectable pre-spawn: YES.** In the parent, before `Process.Start`, `File.Exists(SqliteConnectionFactory.BankPathFor(options))` resolves the bank path **without opening the bank** → return `ExitCode.NoBank` instead of spawning. Placement options: at each composition root (`BackendSessions.cs:174`, `CliSettingsBackend.cs:43`) or at the single chokepoint `BackendLauncher` before `Start` (`:63`/`:112`).

**7. SERVE/RESTART SEAMS — VERIFIED (one line-span correction)**
- `NodeRunner.cs:231-236` `RefuseExistingServerAsync` returns **`ExitCode.PortInUse` (=3)** at **:236** (message "port … is in use by an ai-raccoon server — pass --attach…"); the generic `default:` arm also returns `ExitCode.PortInUse` at **:221-223**. `PortInUse = 3` (`ExitCode.cs:4`). Matches "exit 3 on port held".
- `ServerRestart.cs` **AttachRequired block is :82-84** (`Log.AttachRequired` :83, `return new RestartResult(RestartOutcome.AttachRequired, info.Pid, info.Version)` :84) — the gate sits **before** the token read (`if (!attaching) → AttachRequired`, `:81-84`), so without `--attach` nothing is read/sent. (:85-91 is the `NoToken` transition, not AttachRequired.)
- `RestartOutcome` enum (`RestartOutcome.cs:4-38`, `AttachRequired :26`, `NoToken :29`, `Refused/Unsupported/TimedOut/Foreign/Stopped/Unknown/Nothing`); `RestartResult(Outcome, Pid?, Version?)` (`IServerRestart.cs:18`).
- **Post-#653/#654 shapes are intact — no interference.** `AttachRequired`/`RestartOutcome`/`RestartResult` appear only in `IServerRestart.cs`, `RestartTransition.cs`, `RestartOutcome.cs`, `ServerRestart.cs`, `NodeRunner.cs` (8 coherent references; no duplicate or conflicting redefinition). `RestartRefusal` switch (`NodeRunner.cs:239-267`) maps outcomes to `PortInUse`/`RestartNoToken`/`RestartTokenRefused`/`RestartUnsupportedServer`/`RestartTimedOut` exactly as the plans assume.

**8. CLI SURFACE — VERIFIED**
- `--attach` is defined **twice**: `Setup/Cli/CliCommandTree.cs:59` `AttachOption` (root) and `:65` `ServeAttachOption` (serve's post-verb spelling); both `Option<bool>("--attach")`. Parsed value `RootCliOptions.cs:28` `public bool Attach { get; init; }`; read at `CliArgs.cs:196` (`parseResult.ReadOption(CliCommandTree.AttachOption, …)`); propagated `CliOptionsExtensions.cs:33`; serve resolution `ServeCommands.cs:29/:64` `ResolveAttach` (serve's own wins, else root) and `:85` `NodeCliOptions.Attach`.
- **Unrecognized argument exits with `ExitCode.FailedToParseCliArgs` (=9).** An unmatched/unknown token is a System.CommandLine parse error → lands in `parseResult.Errors` (`CliArgs.cs:127` `DistinctErrors`) → `cliInput.Errors` → `AppRunner.cs:54`/`:76` `if (cliInput.Errors.Count > 0) return ExitCode.FailedToParseCliArgs`. (`InvalidArgument` =15 is for a verb's own semantic validation, returned from command handlers — e.g. `ConfigCommands.cs:42,180` — not for unrecognized tokens.)
- **Removal touches:** `CliCommandTree.cs:59` & `:65` (two options), `RootCliOptions.cs:28`, `CliArgs.cs:196` (+ comments :133/:194), `CliOptionsExtensions.cs:33`, `ServeCommands.cs:29/:64/:85`, `NodeRunner` `NodeLaunchDescriptor.Attaching` + `RefuseExistingServerAsync` + `RestartRefusal` AttachRequired arm (:249-252), `ServerRestart.CycleAsync(…, attaching, …)` + its `AttachRequired` return (:81-84), plus `--attach` test usages (`NodeRunnerTests`, `ServeRestartTests`, `McpTokenGateE2ETests`, etc.).

**9. TEST FIXTURES — VERIFIED-PARTIAL (signing gap)**
- `tests/TestHelpers/Squatter.cs`: raw `TcpListener` loopback (`:25`). **Listener: YES. Request capture: YES** — records the full raw request line + header block (`Requests` :37-45, `TokenHeaderValues` :47-54; ideal for asserting "the token never reached the squatter"). **Programmable HTTP responses: NO** — hardcoded `McpResponse`/`OtherResponse` byte[] (`:17-19`), chosen only by `POST /mcp` vs other (`:96-97`). **Signing: NO.**
- `tests/TestHelpers/FakeRaccoon.cs`: a real `WebApplication`/Kestrel (`:58-63`). **Listener: YES** (true ASP.NET). **Programmable responses: PARTIAL** — `/shutdown` status is a ctor param (`shutdownStatus`), `/observability` `name`/`version` are `StartAsync` args; `/mcp` is a fixed 401 JSON-RPC refusal. **Request capture: PARTIAL** — `ShutdownTokenHeaders`/`ShutdownRequests` (`:29-40`, `:78-88`). **Signing: NO today** — but it is a `WebApplication`, so adding a `/proof` handler that reads a nonce and signs with an injected `ECDsa` is **modest work** (one `MapPost` + a signer).
- **Can they answer a proof challenge (sign a key) with modest work?** `FakeRaccoon` **YES** (extend it — add a `/proof` route + injected `ECDsa` signer). `Squatter` **NO as-is** (raw TCP, hardcoded byte[] responses, no body parse, no signing) — extending it to parse a nonce + ECDSA-sign + emit a dynamic response is more than modest. **A new fixture or a FakeRaccoon extension is needed** for the signing side; keep `Squatter` for the "zero secret bytes to a squatter" capture.

**10. EVENTID BURDEN — VERIFIED**
- **Today's `[LoggerMessage]` id count = 189** (strict `\[LoggerMessage(EventId = N` grep over `src/` excl. obj/bin → 189 occurrences, 189 distinct, zero duplicates). (A loose grep first showed 190/`711`-dup — that was the const `PassReceiptEventId = 711` at `ProjectIdsRepairJob.cs:45` matching the substring; the real attribute is `:137`. Not a duplicate.)
- `docs/reference/logging-event-ids.md:12` declares "**189** `[LoggerMessage]`-attributed" methods — matches.
- **Gate `tests/AiRaccoon.Tests/Unit/Observability/LoggerMessageEventIdTests.cs` would FAIL on an undocumented id**, via two doc-coupled tests: `DocumentedCount_MatchesTheMeasuredCount` (`:66`, regex `\*\*(\d+)\*\* \`\[LoggerMessage\]\`` on the doc vs reflection `Entries().Count`) and `EveryEventIdInSource_FallsInsideADocumentedBlock` (`:80`, every src id must fall in the doc's allocation table). Plus `EventIds_AreUniqueAcrossTheAssemblies` (`:16`) and `EventIdBlocks_DoNotInterleaveBetweenOwners` (`:29`). So any new id forces an update to both the doc's "**189**" prose count **and** its allocation table.

---

## (2) Numbered corrections (where a claim is inaccurate)

1. **Claim 5 — "the bank state dir helper (SqliteConnectionFactory.cs:167-176) … reachable":** the named helper `BankDirectoryFor` is **`private static`** (`SqliteConnectionFactory.cs:166`) — **not directly reachable** from the mint code. The reachable public seam is `BankPathFor` (**public**, `:176`) + `Path.GetDirectoryName(...)`, which is exactly the existing idiom at `MaintenanceCommands.cs:19` and `QuietLogging.cs:30`. Relocation is feasible, but through `BankPathFor`, not `BankDirectoryFor`. (Also the helper spans **:166-174**, with `BankPathFor` at :176 — the claim's "167-176" is a hair wide.)
2. **Claim 1 — "whether any existing ECDsa/ECDsaCng usage exists":** there is **none** in the repo (greenfield). If any plan text implies existing ECDsa code to reuse, that is wrong — but no package is needed since `ECDsa` is in-box.
3. **Claim 7 — "ServerRestart.cs ~:84-89 (AttachRequired)":** the `AttachRequired` block is **:82-84** (return at :84). **:85-91 is the `NoToken` transition**, not AttachRequired. The shape is exactly as assumed; only the line span is off.
4. **Claim 3 framing (minor):** today's probe **already** sends zero secret bytes, so the "zero secret bytes" half of the amended gate is satisfied by the current probe unchanged; only the "nonce-only challenge" half is new behavior (the proof endpoint). The amended wording is a **correction of the old** "zero requests to a squatter", which was false (the probe sends one POST to any listener, squatter included).

---

## (3) P1–P5 area that cannot be built as specified + minimal alternative

**No area is unbuildable** — every claim is feasible against the current code. The single spot where the fixture does not do what a build would need:

- **Identity-proof challenge signer (the test-harness area behind claims 3 & 9):** `Squatter` **cannot** answer a proof challenge (raw TCP, hardcoded responses, no signing). If any work item assumes `Squatter` signs a nonce, that sub-item cannot be built as specified.
  - **Minimal alternative:** extend **`FakeRaccoon`** (a `WebApplication`) with one `MapPost("/proof", …)` handler + an injected `ECDsa` signer — modest work — and keep **`Squatter`** solely for the "zero secret bytes / token-never-reached-the-squatter" capture it already does well. No new crypto package; `System.Security.Cryptography.ECDsa.Create(ECCurve.NamedCurves.nistP256)` + `SignData`/`VerifyData` is in-box (net10.0).

Two sub-minor notes (not blockers): ECDSA key code is greenfield (claim 1) — if it ever derives a key via HKDF it must register under `ContentAddressingSites` per `NoHandRolledCryptoTests`; and token relocation must route through public `SqliteConnectionFactory.BankPathFor` + `Path.GetDirectoryName` (correction #1), updating the `McpTokenFile(string dataRoot)` ctor contract and its 4 production call sites.