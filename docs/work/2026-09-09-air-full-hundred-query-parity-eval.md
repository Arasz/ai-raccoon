# Plan: air-full-hundred-query-parity-eval (high effort)

Task: full 100-query harness-vs-bank parity eval on `project-corpus-100.json` (scope A:
extend ingest buckets to all corpus-targeted projects), then a post-F1 one-harness refactor
(dedup / simplify / compose). Plan acceptance: all packages' ACs checked and met.

Research record: `docs/work/2026-09-09-pr625-evaluation-reading.md` (uncommitted; copies in
lane worktrees). Frozen replication contract (rrfK=60, weights 1/1, limit 8, floor 0.6,
λ=0.1, threshold 0.1, Max, Max3X100, structureAlpha=0.5) does not change; ADDITIONS from
review: model weights revision is frozen too, corpus snapshot-SHA is recorded into
results.json. Census: 76 affected queries (62 silent-empty + 14 crash), not 74.
"100" means 99 scored + 1 stale-filtered = 100 accounted. One tolerance term everywhere:
exact on hashes/hits/contingency/MCC-null-through, mean-F1 ±1e-9. RED-paste discipline
applies to every package, not just P1.
Package numbering is task-local (P1–P4 here); the earlier threshold plan's P3/P4/P5 are unrelated.
Top-level gate: every `- [ ]` below checked with its named gate run.

**P1 Bucket extension + full-100 eval + report**

Scope: extend the merged harness so all 100 queries in `corpora/project-corpus-100.json`
(11 buckets; 83 project / 14 custom / 3 shared scopes) run paired with zero harness errors.
Verified inputs: 76/100 queries affected (62 silent-empty + 14 crash) under the hardcoded
2-bucket rule; 14 custom-scope queries crash harness `query_fts` AND would over-search the
vector leg AND are rejected by the bank leg (`invalid-params`) — the fix is three-legged
(harness FTS + harness Chroma `where` + bank-leg scope mapping; bank `scope=project`
already covers custom labels per `SearchContexts.cs:44-50`); C034's null anchor crashes
`run_eval`. P1 OWNS the shared scope/bucket predicate module (new pure
`resolve_buckets(copy, corpus, explicit)` + one VALID_SCOPES table; precedence explicit
`--buckets` > corpus-header derivation > fail loud, never a frozen fallback); P3 later
moves it into `src` composition without redefining it. `params.json` retains resolved
buckets for audit after P3 moves the input. TDD mandatory. Memory: phased embed+upsert, batch 32, 4000-row Chroma cap,
RSS heartbeat per phase (measured headroom: 3.1 GB/914-row ingest, 4.8 GB/10-query eval).

- [ ] AC1 — Every corpus-targeted bucket ingested or explicitly excluded with stated reason; no query silently blind. Gate: ingest with `--corpus project-corpus-100.json` exits 0 + `test_corpus_buckets_covered_or_excluded` (every query projectId ∈ `params.json buckets` or the exclusion manifest, which is closed to seed-equality with header `excludedProjects`) + full ingest test file green. `--corpus` interaction with `--copy` and the `params.json` buckets format are specified in the P1 lane report.
- [ ] AC2 — 99 queries run paired with zero harness errors; C034 stale-filtered pre-`run_eval` (100 = 99 scored + 1 accounted). Gate: evaluate exits 0, `summary.n == 99`, `set(staleAnchors) == {"C026","C034"}` with C026 membership re-proven by the anchor gate at build (never a baked constant), `eval_gate_failures(out) == []`; includes the three-legged `custom→project` mapping (harness `_scope_predicate` + `_chroma_where` else-branch + bank-leg `build_airaccoon_fn`), plus a vector-side custom-scope predicate test (Chroma `where` vs bank SQL predicate) so a half-fix cannot go green silently.
- [ ] AC3 — `report.py` emits the eval markdown (per-query F1, hit-rates, agreement MCC, gap discussion from `fts_hit`/`vector_hit`). SERIAL ORDERING NOTE: P1 builds the aggregate counts + provisional counts table first (it lands first); P2 refines them into the single classified taxonomy (embedding|fusion|unrecoverable|none|unknown incl. structure reading) reusing the same numbers — never a second taxonomy, never redefined semantics. Gate: report CLI + report tests asserting five section headers, rendered gap aggregates, data-driven bucket prose (named test; today `report.py:53` hardcodes bucket prose so the test starts RED), exclusion disclosure, and the phase-2 recompute-path line (hash-preserving golden allows post-hoc relevance metrics; singleton-F1 is a parity verdict, not a relevance verdict).
- [ ] AC4 — C026/C034 handled by warn-and-record, verified. Gate: run log shows `WARNING: 2 stale anchors`, `results.json staleAnchors == ["C026","C034"]`, `test_null_anchor_filtered_to_stale_not_crash` (C034 filtered pre-`run_eval`), anchor-resolution gate proves C026 at build.
- [ ] Tests first: bucket-derivation-from-header unit test; multi-project (third-bucket) parity-probe regression; custom-scope FTS-vs-bank-SQL parity test; null-anchor filter test; stale-anchor report test; gap-aggregate report test; exclusion-manifest test. Every new gate broken on purpose once (RED pasted) before trusting GREEN.

**P1 close-out (review d-756 + determinism double-run) — blocks golden freeze; P2 dispatch gated on C1–C7 or the P2 AC1/AC2 amendments below**

- [ ] C1 (MUST, F1) — Deterministic vector leg. Root cause MEASURED on the frozen store: Chroma's content-collection top-k truncates an exact-distance tie group at the requested k and the surviving subset is process-dependent (same-process repeats stable; query embedding and structure leg bit-identical across fresh processes; C066's tie group = 17 items at d=2^-24 spanning the k=100 cut; k=200 covers it and is byte-stable). Fix: tie-complete window expansion in `retrieve.FusionRetriever.vector_leg` for BOTH collections — grow the request while the (window+1)-th distance equals the window-th (tie group not fully covered), bounded by collection count, then sort by (distance, hash) and cut at `window`. Gate: unit test with a fake collection whose returned tie subset varies with k (RED first) + two fresh-process full-100 runs byte-identical on per-query hashes under the plan tolerance. Fallback if expansion ever fails to stabilize (huge tie groups): exact in-process cosine over the filtered scope, with the ANN→exact semantic change called out explicitly. Evidence: `docs/work/2026-09-10-p1-determinism-finding.md`.
- [ ] C2 (SHOULD, F2/F3) — Exclusion manifest truth + report wording. `build_project_corpus.py` splits its per-project counts into committed (`scope IN ('project','custom')`) vs shared rows and rewrites each reason (raw-spelled committed rows can never match the project predicate; its shared rows are global, remain ingested and ARE served by shared/all — verified against `ContextFilterProvider.cs:23` + `MemorySql.cs:813`); regenerate the corpus (checked-in regeneration-equality test stays green); `report.py` renders `resolvedBuckets` as the resolved project buckets and lists extra observed shared-tier spellings separately. The ingest rule (`OR scope='shared'`, no project filter) stays as-is: correct parity.
- [ ] C3 (SHOULD, F4) — Provenance completeness: eval-side model-revision check (`build_harness_fn`/`main` compares to store `params.modelRevision`, fails loud); `modelRevision`/`modelBytes` into `results.json`; `copyPath`/`copySnapshotSha256` into `params.json` (+ report line); all added to P3 AC4's provenance allow-list.
- [ ] C4 (SHOULD, F5) — `collect_ac_evidence.py`'s model-pin check compares the recorded revision to `ingest.PINNED_MODEL_REVISION` (today any non-empty string passes — a gate nobody has seen fail); re-run the collector and paste its output into the lane report's AC-evidence section together with the double-run diff table, RSS lines, and anchor/stale evidence.
- [ ] C5 (NOTE fixes) — Lane report: correct the 135-row breakdown (106 raw-spelled committed rows — `job-search-ai-assistant` 77 custom + 29 project; `aib` has zero committed — + 29 NULL-scope (26 ai-raccoon + 2 job-search + 1 jsaa)), note the deliberate `test_partition_null_anchors` assertion change inside `1b6ce543` and that one RED paste was argparse-not-behavior (F8), tighten the dedupe-neutrality sentence (`3f76c7890d5a` appears in 0/198 served sets — F10); `slice_copy` explicit blank `--buckets ""` → exit 2 (F11); committed `memwatch.py` must stream the child's stdout, never clip it (the /tmp version prints only `out[-6000:]` — found during probing; note the eval artifacts were unaffected because they are file-written).
- [ ] C6 — Store-params refresh WITHOUT re-embedding. Header/metadata-only changes (C2 manifest, C3 copy SHA) must not cost the 13 h ingest. Implement the minimal sound mechanism: ingest auto-skips embed+upsert when the fresh row id set + text hashes + model revision match the store (still re-running dedupe, FTS rewrite, params, verify), OR a documented `--refresh-params` mode; test that the fast path is REFUSED when rows changed. Fallback if rejected: full re-ingest and state the cost.
- [ ] C7 — Golden freeze: re-run eval TWICE on the refreshed store after C1–C6 (≈3 min each); require byte-identical per-query hashes under the tolerance; then name and commit the golden `results.json` (+ run-2 file + report + lane report + finding docs). Only then P1 is done.
- [ ] C8 — P2 dispatch gate: C1–C7 done, or the amended P2 AC1/AC2 approved; P2 pure-function work may start pre-golden (no golden dependency).

**P2 Full-100 parity eval with repeat-run spread, leg-gap aggregation, enforced anchor accounting, committed memory guard**

Measurement-only, additive; pure fns merge conflict-free. Shared-surface edits (`evaluate.main()`, `report.render()`) order last with P2 rebasing onto merged shapes; no package redefines `summarize()` semantics.

- [ ] AC1 — Repeats report spread. AMENDED after the measured run1-vs-run2 diff (C045/C066 served-set divergence on ONE frozen store): the original premise "repeats on one store measure jitter-only by construction" is FALSIFIED — one-store repeats DO carry information (per-query instability), so `--repeats N` must diff per-query served SETS and record unstable query ids, not only min/max aggregates; after C1 the one-store determinism check becomes a regression gate (must be exact), and N≥3 independent repeats with a FRESH server copy per repeat carry the spread bar. Gate: `test_summarize_repeats_min_max` (+ degenerate N=1) for pure `summarize_repeats`; `results.json["repeats"]` pins weights revision/bytes; report renders `mean [min–max]`; the gate states what variance repeats guard (weight drift, server nondeterminism, bank drift).
- [ ] AC2 — Gap decomposed from existing columns only. AMENDED (F9): classification is computed against the CANDIDATE WINDOW (max(limit*3,100)), not each leg's top-8 — a c-cell row whose leg held the anchor at window-rank 9–100 must not be labelled "leg missed" — and the taxonomy must split "no leg had it in the window" from "a leg had it and fusion dropped it" (the 14 `c_fts_only` rows are NOT all embedding-gap; the fusion-drop subset is the actionable part). Gate: `test_gap_columns_classification_table` + `test_gap_counts_sum_to_paired` + per-label oracle rows for pure `gap_columns`/`aggregate_gaps` (embedding|fusion|unrecoverable|none|unknown incl. structure reading; bank leg has no split so classification is harness-relative AND computed only on the c-cell — bank-hit/harness-miss — never misattributing agreement as deficit); `unknown` share capped (bar: ≤5%, else the classifier not the data failed); conservation cells sum to paired. Report gains the single gap table (P1 prose consumes it). No new retrieval instrumentation.
- [ ] AC3 — Anchor verification never silent. Gate: `test_anchor_verdict_refuse_warn_clean` + `test_stale_anchors_always_recorded` for pure `anchor_verdict`; stale count joins the one-line stdout summary; `results.json` always carries `staleAnchors` even when empty on the success path (on gate failure `main` exits before writing — either write partial results on failure or scope the claim; lane decides, test pins the choice); report repeats the count.
- [ ] AC4 — Memory guardrail committed and fail-loud (labelled a backstop, not a guardrail: 2 s sampling cannot catch inter-sample spikes). Gate: `test_memwatch_runs_as_module` + a LIVE-kill proof (spawn a hog under a tiny cap, assert exit 99 + tree death — a pure kill-decision test cannot observe grandchildren/orphans/sampling-gap defects) + a stdout-passthrough test (the probe version clipped child output to its last 6000 chars — C5) for new `scripts/retrieval_tuning/memwatch.py` (import-safe argparse+main, default cap 12 GB, SIGTERM-the-tree, prints peak+cap+RAM; sampling limitation documented in --help). Mutual exclusion: heavy lanes (ingest/eval/refactor-rerun) never run concurrently on one box — 2×12 GB caps exceed it; serialize or gate on joint RSS.

**P3 Post-F1 retrieval-harness consolidation (deduplicate, simplify, compose)**

Runs AFTER F1 numbers land for landing (needs the frozen `results.json` golden + corpus snapshot-SHA; also contends for the same HF/Chroma/server resources). Read-only prep (AC1 inventory/merge-map, AC3 fixture-scoped refresh work) is explicitly permitted pre-F1 — it needs no golden and touches no heavy resources. Follow `.ai-badger/skills/scripts-tooling-refactor/SKILL.md`. Deliberate fork excluded: `src/scoring.py` nDCG/MRR vs harness singleton-F1/MCC (different questions).

- [ ] AC1 — Duplication merged per corrected inventory (7 bucket/scope-membership spellings: `load_rows` WHERE, `verify_store` WHERE, `_scope_predicate`, probe legs, `_chroma_where`, slice keep-rule, PLUS `scripts/src/retrieval_tuning/corpus.py` `VALID_SCOPES`/`load_corpus` (review F6 — a second table whose validator rejects `custom`; route it through `scopes` or document explicitly why it is off the eval path) → one builder in `src` with SQL+Chroma emitters covering BOTH per-query predicates and ingest-rule set-membership; one `VALID_SCOPES` + default-scope table; one corpus loader; one `scratch_server()` composition). P1's `resolve_buckets` + VALID_SCOPES table move here unchanged in behavior (P1 owns creation, P3 owns relocation — no redefinition). Gate: `pytest scripts/tests` green before+after + checked-in `docs/work/p3-merge-map.md` reviewed against the corrected 7-spelling inventory (a map checked against a 3-count is self-certifying).
- [ ] AC2 — JSON owns behavior constants. Gate: `rg` for each moved constant returns zero hits in logic files, enforced by `test_no_hardcoded_knobs.py` whose constant list is DERIVED from the JSON keys (a hand list rots on the next literal). Moves: buckets → `data/buckets.json`; PARAMS/settings defaults/limit/model/bm25 weights → `data/knobs.json`; grader trios → `data/graders.json`; corpus seeds/frames/allowists → `data/corpora/*.json`. (Corpus generators stay two — different contracts — sharing one anchor-resolution lib.)
- [ ] AC3 — Refresh script `scripts/refresh-retrieval-corpora.py` (thin wrapper, logic in `src`). Gate: exit-code contract test on fixture banks (0 clean / 1 anchor drift / 2 snapshot mismatch / 3 smoke regression); outputs regenerated corpora + `refresh-report.json`; determinism = fixed SEEDs, stable sorts, no timestamps; anchor re-verification fails loud.
- [ ] AC4 — Behavior preservation. Gate: the pre-golden DOUBLE-RUN determinism proof MOVED to P1 close-out C7 (ownership: it gates P1's golden, not P3); P3 keeps only the post-refactor rerun diff vs the frozen golden under ONE tolerance term — exact on hashes/hits/contingency/MCC-null-through, mean-F1 ±1e-9 (viable again once C1 lands; if C1 were rejected the term alternates to anchor-exact + summary-exact) — with `sessionId` + P1/P2 provenance pins (incl. copy `copySnapshotSha256`, C3) allow-listed (NOT `ingestedAt`, which lives in `params.json`, and NOT `staleAnchors`: drift moves paired/hit-rates, so the rerun asserts the frozen corpus snapshot-SHA first and any drift fails as drift, never as refactor noise) + full suite + CI lane + heavy local gate green. Expected values derived by executing legacy code on fixtures, never by reading; old-vs-new CLI parity harness diffs stdout/stderr/exit/artifacts.

**P4 Integration (always last)**

Cross-package joins, checked on the combined tree — parts that pass alone fail together.

- [ ] AC1 — Shared-surface merge order held: `resolve_buckets` lands before P2's `main()` wiring; P2 rebases onto it; P3 rebases onto both; each join re-runs the touched suites on the merged tree (not the lane trees).
- [ ] AC2 — Post-integration full-100 eval (after P3) reproduces the F1 numbers under the single tolerance term of P3 AC4 as amended (exact hashes/hits/contingency/MCC-null-through, mean-F1 ±1e-9 — not a second term; provable only because the P1-close-out C7 double-run fixed the vector-leg nondeterminism) AND carries P2's repeats/spread/gap/anchor blocks — one `results.json` proving both the numbers and the consolidation.
- [ ] AC3 — Fresh-bank end-to-end (P3 AC3 gate): `make_memory_copy.py --verify-only` → refresh script → ingest → evaluate → report green from a clean checkout state, proving the scripted solution a stranger can run next time. Offline/binary assumptions stated up front (1.6 GB cached HF weights + `ai-raccoon` binary present) so a first run fails on wiring, never on download.
- [ ] AC4 — Review-round loop closed: implementation-review MoE findings folded or filed, QA test-honesty verdict recorded, CI green on the PR, Worktree `start` created holds no unmerged/uncommitted work at `finish`.
