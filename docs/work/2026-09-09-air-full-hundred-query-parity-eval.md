# Plan: air-full-hundred-query-parity-eval (high effort)

Task: full 100-query harness-vs-bank parity eval on `project-corpus-100.json` (scope A:
extend ingest buckets to all corpus-targeted projects), then a post-F1 one-harness refactor
(dedup / simplify / compose). Plan acceptance: all packages' ACs checked and met.

Research record: `docs/work/2026-09-09-pr625-evaluation-reading.md` (uncommitted; copies in
lane worktrees). Frozen replication contract (rrfK=60, weights 1/1, limit 8, floor 0.6,
λ=0.1, threshold 0.1, Max, Max3X100, structureAlpha=0.5) does not change; ADDITIONS from
review: model weights revision is frozen too, corpus snapshot-SHA is recorded into
results.json. Census: 76 affected queries (62 silent-empty + 14 crash), not 74.
"100" means 99 scored + 1 stale-filtered = 100 accounted. One tolerance term everywhere, RESOLVED by measurement (C12): with the quiet-scratch recipe (C11) the full artifact is byte-identical across fresh-process runs on BOTH legs (4/4, ordered-exact), so the gate is exact on hashes/hits/contingency/MCC + mean-F1 ±1e-9; the tie-tolerant bank rule applies only if a production-default recipe is ever used. RED-paste discipline
applies to every package, not just P1.
Package numbering is task-local (P1–P4 here); the earlier threshold plan's P3/P4/P5 are unrelated.
Top-level gate: every `- [ ]` below checked with its named gate run.

**P1 Bucket extension + full-100 eval + report**

Scope: extend the merged harness so all 100 queries in `corpora/project-corpus-100.json`
(11 buckets; 83 project / 14 custom / 3 shared scopes) run paired with zero harness errors.
Verified inputs: 76/100 queries affected (62 silent-empty + 14 crash) under the hardcoded
2-bucket rule; 14 custom-scope queries crash harness `query_fts` AND would over-search the
vector leg AND are rejected by the bank leg (`invalid-params`) — the fix is three-legged
(harness FTS + harness Chroma `where` + bank-leg scope mapping; bank `scope=project`
already covers custom labels per `SearchContexts.cs:44-50`); C034's null anchor crashes
`run_eval`. P1 OWNS the shared scope/bucket predicate module (new pure
`resolve_buckets(copy, corpus, explicit)` + one VALID_SCOPES table; precedence explicit
`--buckets` > corpus-header derivation > fail loud, never a frozen fallback); P3 later
moves it into `src` composition without redefining it. `params.json` retains resolved
buckets for audit after P3 moves the input. TDD mandatory. Memory: phased embed+upsert, batch 32, 4000-row Chroma cap,
RSS heartbeat per phase (measured headroom: 3.1 GB/914-row ingest, 4.8 GB/10-query eval).

- [ ] AC1 — Every corpus-targeted bucket ingested or explicitly excluded with stated reason; no query silently blind. Gate: ingest with `--corpus project-corpus-100.json` exits 0 + `test_corpus_buckets_covered_or_excluded` (every query projectId ∈ `params.json buckets` or the exclusion manifest, which is closed to seed-equality with header `excludedProjects`) + full ingest test file green. `--corpus` interaction with `--copy` and the `params.json` buckets format are specified in the P1 lane report.
- [ ] AC2 — 99 queries run paired with zero harness errors; C034 stale-filtered pre-`run_eval` (100 = 99 scored + 1 accounted). Gate: evaluate exits 0, `summary.n == 99`, `set(staleAnchors) == {"C026","C034"}` with C026 membership re-proven by the anchor gate at build (never a baked constant), `eval_gate_failures(out) == []`; includes the three-legged `custom→project` mapping (harness `_scope_predicate` + `_chroma_where` else-branch + bank-leg `build_airaccoon_fn`), plus a vector-side custom-scope predicate test (Chroma `where` vs bank SQL predicate) so a half-fix cannot go green silently.
- [ ] AC3 — `report.py` emits the eval markdown (per-query F1, hit-rates, agreement MCC, gap discussion from `fts_hit`/`vector_hit`). SERIAL ORDERING NOTE: P1 builds the aggregate counts + provisional counts table first (it lands first); P2 refines them into the single classified taxonomy (embedding|fusion|unrecoverable|none|unknown incl. structure reading) reusing the same numbers — never a second taxonomy, never redefined semantics. Gate: report CLI + report tests asserting five section headers, rendered gap aggregates, data-driven bucket prose (named test; today `report.py:53` hardcodes bucket prose so the test starts RED), exclusion disclosure, and the phase-2 recompute-path line (hash-preserving golden allows post-hoc relevance metrics; singleton-F1 is a parity verdict, not a relevance verdict).
- [ ] AC4 — C026/C034 handled by warn-and-record, verified. Gate: run log shows `WARNING: 2 stale anchors`, `results.json staleAnchors == ["C026","C034"]`, `test_null_anchor_filtered_to_stale_not_crash` (C034 filtered pre-`run_eval`), anchor-resolution gate proves C026 at build.
- [ ] Tests first: bucket-derivation-from-header unit test; multi-project (third-bucket) parity-probe regression; custom-scope FTS-vs-bank-SQL parity test; null-anchor filter test; stale-anchor report test; gap-aggregate report test; exclusion-manifest test. Every new gate broken on purpose once (RED pasted) before trusting GREEN.

**P1 close-out (review d-756 + determinism double-run) — blocks golden freeze; P2 dispatch gated on C1–C7 or the P2 AC1/AC2 amendments below**

CLOSE-OUT STATUS (2026-09-10): **C1–C11 DONE.** C1–C7 via lane d-757 (merged `44546818`); C9–C11 via lane d-758 round 2 (merged `8b45e5d4`). Golden frozen on the quiesced-scratch recipe: `docs/work/results-f1.json` (run C) + `results-f1-run2.json` (run D), byte-identical modulo sessionId on BOTH legs. Numbers: harness 0.6768/0.1582, bank 0.8081/0.1874, MCC 0.5955, cont 65/2/15/17, stale=[C035], gaps c_cell=15 (14 fts-only + 1 both). **C10 verdict: per-spec (golden unchanged)** — shared rows C019/C065/C081 pass dedupe/RRF/affinity/floor and drop at Take(8); the divergence is upstream in the vector leg, whose cause is the measured EMBEDDING SEAM (bank manifest pins CLS pooling; the HF snapshot has no sentence-transformers config so the harness falls back to mean pooling; stored-vector cos 0.50–0.63 for identical text) → C13. C9: report recomputes debris 23/clean 76 strata + composition disclosure + `staleAnchors ∩ scored == ∅`. C11: `make_quiesced_scratch.py` (tested, refuses row drift) + `--stability` snapshot + RUNBOOK; the helper's freshly built base sha is `eeb431a3…` (2.1 s from the pinned copy; `aec21bf7…` was d-757's manual build — prefer the helper's output, row counts must match the source). F1–F5, F7, F8, F10, F11 closed; F6→P3 AC1, F9→P2 AC2. Test baseline (stash-proved): full `scripts/tests` = 551 passed, 8 skipped, 5 failed, ALL pre-existing (3 unrelated artifacts + 2 needing optuna).

- [x] C1 (MUST, F1) — Deterministic vector leg. Root cause MEASURED on the frozen store: Chroma's content-collection top-k truncates an exact-distance tie group at the requested k and the surviving subset is process-dependent (same-process repeats stable; query embedding and structure leg bit-identical across fresh processes; C066's tie group = 17 items at d=2^-24 spanning the k=100 cut; k=200 covers it and is byte-stable). Fix: tie-complete window expansion in `retrieve.FusionRetriever.vector_leg` for BOTH collections — grow the request while the (window+1)-th distance equals the window-th (tie group not fully covered), bounded by collection count, then sort by (distance, hash) and cut at `window`. Gate: unit test with a fake collection whose returned tie subset varies with k (RED first) + two fresh-process full-100 runs byte-identical on per-query hashes under the plan tolerance. Fallback if expansion ever fails to stabilize (huge tie groups): exact in-process cosine over the filtered scope, with the ANN→exact semantic change called out explicitly. Evidence: `docs/work/2026-09-10-p1-determinism-finding.md`.
- [x] C2 (SHOULD, F2/F3) — Exclusion manifest truth + report wording. `build_project_corpus.py` splits its per-project counts into committed (`scope IN ('project','custom')`) vs shared rows and rewrites each reason (raw-spelled committed rows can never match the project predicate; its shared rows are global, remain ingested and ARE served by shared/all — verified against `ContextFilterProvider.cs:23` + `MemorySql.cs:813`); regenerate the corpus (checked-in regeneration-equality test stays green); `report.py` renders `resolvedBuckets` as the resolved project buckets and lists extra observed shared-tier spellings separately. ALSO (external validation #5b): the Method line "overrides the corpus searchLimit=5" is false — the corpus records `searchLimit: 8` for all 100 queries (Counter verified); state the real uniform-8 fact. The stale-anchor sentence is already correct (verified: absent anchors score into d, null-anchored rows filtered pre-eval) — no change. The ingest rule (`OR scope='shared'`, no project filter) stays as-is: correct parity. REGENERATION CONSEQUENCE (refreshed pair, measured): the regenerated corpus is a renumbered version — 73/100 queries identical by id, 96/100 query texts shared, snapshotSha256 moved f2cb1210→e0434a72 (= the bank copy's own SHA, so corpus↔copy↔store provenance now agrees), staleAnchors moved [C026,C034]→[C035] (C035 = content-targeted null anchor, filtered; no absent-anchor row remains), and the numbers re-baselined (harness 0.677/0.158 vs bank 0.798/0.185, MCC 0.6205, cont 65/2/14/18). Record the old pair as historical in the lane report; the refreshed pair is the golden.
- [x] C9 — Query-composition disclosure (external validation #1 + refreshed pair, MEASURED). Add to the eval report a deterministic stratification recomputed from the CURRENT (refreshed) corpus, never a hardcoded id list (ids shift with regeneration). Current refreshed reading (recompute, do not hardcode): debris n=23 harness 0.6087 / bank 0.6957; clean n=76 harness 0.6974 / bank 0.8421 (round-2 recompute; supersedes the earlier 0.8289) — disclosing that relevance-flavoured readings ("bank finds what harness misses", embedding-gap narrative) are composition-sensitive while the parity/pipeline reading stands. P2's taxonomy must consume the same stratification when publishing gap labels. Replace the obsolete C026 cell-d sensitivity (the refreshed corpus has NO stale-scored row — C035 is filtered) with the asserted invariant `staleAnchors ∩ scored == ∅` in the evidence.
- [x] C10 (MUST-investigate before freeze, external validation #3 + P2 dependency) — Shared-scope fusion loss: the current corpus's shared-scope rows (refreshed pair: C019/C065/C081; previously C020/C065/C081 — ids shift with regeneration, select by targetScope=shared) are final harness misses with `fts_hit=1` on ALL three (external probe: the anchor is rank 1 in the shared FTS result, bm25 −34.6/−28.1/−42.5) and `vector_hit` 0/0/1; the bank hits 3/3. A top-8 leg hit dropped by the fused pipeline is either a replication defect (dedupe/floor λ/affinity/RRF path) or a per-spec behaviour that must be explained. Repro: `/tmp/p1-red/c10_shared_leg_trace.py` (sibling session, exact `_retrieve_with_limit` replica with per-stage prints; copy into the branch with the evidence). If a defect, fix per the frozen contract semantics and include it in the C7 re-run BEFORE the golden freezes; if per-spec, document why in the report and feed the classification to P2 AC2 (these rows must NOT be labelled "embedding-gap evidence").
- [x] C3 (SHOULD, F4) — Provenance completeness: eval-side model-revision check (`build_harness_fn`/`main` compares to store `params.modelRevision`, fails loud); `modelRevision`/`modelBytes` into `results.json`; `copyPath`/`copySnapshotSha256` into `params.json` (+ report line); all added to P3 AC4's provenance allow-list.
- [x] C4 (SHOULD, F5) — `collect_ac_evidence.py`'s model-pin check compares the recorded revision to `ingest.PINNED_MODEL_REVISION` (today any non-empty string passes — a gate nobody has seen fail); re-run the collector and paste its output into the lane report's AC-evidence section together with the double-run diff table, RSS lines, and anchor/stale evidence.
- [x] C5 (NOTE fixes) — Lane report: correct the 135-row breakdown (106 raw-spelled committed rows — `job-search-ai-assistant` 77 custom + 29 project; `aib` has zero committed — + 29 NULL-scope (26 ai-raccoon + 2 job-search + 1 jsaa)), note the deliberate `test_partition_null_anchors` assertion change inside `1b6ce543` and that one RED paste was argparse-not-behavior (F8), tighten the dedupe-neutrality sentence (`3f76c7890d5a` appears in 0/198 served sets — F10); `slice_copy` explicit blank `--buckets ""` → exit 2 (F11); committed `memwatch.py` must stream the child's stdout, never clip it (the /tmp version prints only `out[-6000:]` — found during probing; note the eval artifacts were unaffected because they are file-written).
- [x] C6 — Store-params refresh WITHOUT re-embedding. Header/metadata-only changes (C2 manifest, C3 copy SHA) must not cost the 13 h ingest. Implement the minimal sound mechanism: ingest auto-skips embed+upsert when the fresh row id set + text hashes + model revision match the store (still re-running dedupe, FTS rewrite, params, verify), OR a documented `--refresh-params` mode; test that the fast path is REFUSED when rows changed. Fallback if rejected: full re-ingest and state the cost.
- [x] C7 — Golden freeze: re-run eval TWICE on the refreshed store after C1–C6 (≈3 min each) USING THE QUIET SCRATCH RECIPE (C11: watches disabled, row-stability asserted; the contaminated runs 1–3 are evidence, not the baseline); require the C12 tolerance to hold; then name and commit the golden `results.json` (+ run-2 file + report + lane report + finding docs). Only then P1 is done.
- [x] C8 — P2 dispatch gate: C1–C7 done, or the amended P2 AC1/AC2 approved; P2 pure-function work may start pre-golden (no golden dependency).
- [x] C11 — Eval hygiene: watch-freeze + no-write assertions (external validation #4; CONFIRMED IN THIS CAMPAIGN). Scratch copies used for eval must have `watch.enabled.*` disabled/frozen before the run, and each run asserts row-stability (`entries` count + `max(created_at/updated_at)` unchanged before/after; a small bookkeeping-only WAL is expected from access bumps). MEASURED CONTAMINATION (runs 1–3, scratch `-f1`): the scratch server ran `compact the bank` (vacuum, 3968 ms) and `pending-embed` maintenance MID-RUN (serve.log) and the scratch ended at 53,637 entries vs the source's 56,457 with `max(updated_at)` = run time — the bank leg searched a mutating DB, and bank hit-rate moved 0.798 (runs 1/2) → 0.869 (run 3) with zero code change. The quieted scratch (`-quiet`, `watch true: 0`) stayed row-pristine during its run (56,457 entries, source `max(updated_at)`). Golden = the quiet pair; runs 1–3 are C11 evidence, never baseline. C3's copy SHA + these counters land in params/results provenance. (If d-757 closes before this lands, implement C9–C11 as the close-out round 2.)
- [ ] C12 — Bank-leg tie-jitter: ROOT-CAUSED AND RESOLVED (quiet recipe → 4/4 byte-identical, both legs, ordered). Refreshed run1-vs-run2 (contaminated scratch) diff: harness 0 (C1 holds); bank 12 = 10 adjacent reorders + 2 rank-8/8 set swaps (C069/C070), counts equal, anchors present, metrics identical — later run-to-run bank hits also moved (0.798→0.869) with no code change. Bank final sorts are all total (`StructureFusion.cs:60-61`, `ReciprocalRankFusion.cs:82-92`, `SourceAffinityRanker.cs:48-56`) → swaps are INPUT score/order changes; FTS SQL's missing hash tiebreak (`MemorySql.cs:113`, latent gap vs the harness port) ruled out (swapped pairs are not exact bm25 ties). MECHANISM CONFIRMED = mid-run bank mutation on the scratch DB (C11: vacuum + pending-embed + watches during the eval; entry count 56,457→53,637) — with the quiet scratch (watches disabled, row-stability asserted) FOUR fresh-process runs (A/B/C/D) are byte-identical per-query on both legs, ordered, summary equal. ONNX intra-op threading (`embedding.threads` unset → 5 threads; sibling source-read) is a plausible secondary for production-default recipes but NOT triggered here — `embedding.threads=1` is documented belt-and-braces, not required. Golden gate (quiet recipe): exact hashes/hits/contingency/MCC (ordered, both legs) + mean-F1 ±1e-9; if a production-default-recipe run is ever compared, fall back to the tie-tolerant bank rule (set-equal; boundary swaps allowed only when count equal, hit/f1 unchanged, anchor present in both, ids recorded). If any run's tie moves an ANCHOR (hit changes), stop and report — never tolerance-blessed. Upstream notes to record: ONNX default-threading order-instability risk; bank FTS SQL no-tiebreak latent gap.
- [ ] C13 (OPEN — OWNER DECISION, from C10; mechanism CORRECTED by external validation — the pooling theory was WRONG and is retracted) — Embedding seam = **ONNX-vs-HF model conversion**, not pooling. Evidence (triangle test, measured; probes at /tmp/p1-red/): the harness ALREADY pools CLS — llama-index's `get_pooling_mode` defaults to `'cls'` when the repo has no pooling config (`utils.py:79-98`) and `base.py:150` feeds it to SentenceTransformer; the live model object reports `pooling_mode='cls'` and its output equals the model-card recipe (`last_hidden_state[:,0]`, fp32, repaired position_ids) at **cos 1.00000** on 8 bank texts — so `pooling='cls'` is a NO-OP (and the installed llama-index refuses the parameter: "pooling is deprecated"); store == current HFE path at 1.00000; the bank's own ONNX reproduces the bank's stored vectors at **0.9826** (0.945–0.9999); ONNX-vs-HF on the same text = **0.5934** (padding irrelevant: 1.00000). So the two deployed systems embed the same text differently because the bank runs an ONNX export while the harness runs the HF safetensors path. Leads: bank manifest pins `source.revision: 'main'` (unpinned) vs harness snapshot `cb950dc8…` (the weights file is not covered by the matching config/tokenizer provenance); and/or the ONNX export's positional handling differs from the repaired HF path. Residual 1.7% on the bank match is likely the bank's C# tokenizer (separate, smaller seam). Options: (a) harness embeds with the bank's exact ONNX file (removes the seam by construction; converts the comparison into a fusion-isolation, changes what the harness is); (b) pin/reconcile the ONNX's source revision and re-export; (c) leave it + state the seam as a measured model-conversion divergence between two real systems. RECOMMENDATION: (c) for this task — the seam is a genuine system difference (not a replica mistake like the alleged pooling bug), now measured and disclosed; (a) is the right control if someone wants the fusion isolated. NO re-embed — the pooling fix is dead. Report requirement: the seam disclosure must carry the corrected mechanism + numbers (`vector_hit=1` on 8/99; fts=1/vec=0 on 78/99; 14/15 c-cell; ONNX-vs-HF cos 0.5934), never the retracted pooling explanation.
- [ ] C14 (correction, MUST land before task close) — the retracted pooling mechanism is committed in two artifacts: `scripts/retrieval_tuning/llamaindex_harness/report.py:216-221` ("falls back to that library's mean-pooling default") and `docs/work/2026-09-10-p1-c10-shared-fusion-trace.md` §4-§5 ("CLS vs mean pooling is the divergence"). Replace with the measured ONNX-vs-HF conversion divergence (0.5934; bank-ONNX self-consistency 0.9826; harness already CLS, cos 1.00000 vs the model-card recipe) + an explicit retraction note; regenerate the eval report from the golden. Owner: post-P2 round (the P2 lane is mid-flight and cannot be messaged).

**P2 Full-100 parity eval with repeat-run spread, leg-gap aggregation, enforced anchor accounting, committed memory guard**

P2 STATUS (2026-09-10): **DONE** via lane d-759, merged as `d396111b` (plus the C14 correction `12a72dfb`). AC1: `--repeats 3` from fresh quiesced-base copies — rows ordered-exact equal to the frozen golden on BOTH legs, unstable ids `[]` on harness+bank, only additive `summary.gaps.taxonomy` + repeats blocks, peaks 7207/6522/6451 MB, base SHA unchanged. AC2: window-level taxonomy — golden c-cell **15/15 fusion** (embedding 0, unrecoverable 0, unknown 0.0%); **6/15 anchors sat at leg-window rank 9–100** (top-8 diagnostics would have mislabelled them); shared oracle C019/C065/C081 fusion. AC3: `anchor_verdict` + stale always recorded. AC4: memwatch verified real (live tree-kill RED; module-run/cap/help gates filled). Report source = `results-f1-repeats.json`; the frozen golden files stay the numeric reference. Full suite 573 passed / 6 pre-existing failures (same on base `8b45e5d4`).

Measurement-only, additive; pure fns merge conflict-free. Shared-surface edits (`evaluate.main()`, `report.render()`) order last with P2 rebasing onto merged shapes; no package redefines `summarize()` semantics.

- [x] AC1 — Repeats report spread. AMENDED after the measured run1-vs-run2 diff (C045/C066 served-set divergence on ONE frozen store): the original premise "repeats on one store measure jitter-only by construction" is FALSIFIED — one-store repeats DO carry information (per-query instability), so `--repeats N` must diff per-query served SETS and record unstable query ids, not only min/max aggregates; after C1 the one-store determinism check becomes a regression gate (must be exact), and N≥3 independent repeats with a FRESH server copy per repeat carry the spread bar. Gate: `test_summarize_repeats_min_max` (+ degenerate N=1) for pure `summarize_repeats`; `results.json["repeats"]` pins weights revision/bytes; report renders `mean [min–max]`; the gate states what variance repeats guard (weight drift, server nondeterminism, bank drift).
- [x] AC2 — Gap decomposed from existing columns only. AMENDED (F9): classification is computed against the CANDIDATE WINDOW (max(limit*3,100)), not each leg's top-8 — a c-cell row whose leg held the anchor at window-rank 9–100 must not be labelled "leg missed" — and the taxonomy must split "no leg had it in the window" from "a leg had it and fusion dropped it" (the 14 `c_fts_only` rows are NOT all embedding-gap; the fusion-drop subset is the actionable part). C10 feeds this directly: the three shared-scope rows (C020/C065/C081, FTS rank-1 hits, bank 3/3) are fusion-drop, never "embedding gap", and query composition (C9) qualifies what the aggregate gap means at all. The report's seam disclosure must carry the C13 numbers (harness `vector_hit=1` on 8/99 rows; fts=1/vec=0 on 78/99; 14/15 c-cell rows are `fts=1, vec=0`; ONNX-vs-HF same-text cosine 0.5934) and the corrected mechanism (ONNX-vs-HF conversion, NOT pooling — C13/C14) — a pointer is not enough. Gate: `test_gap_columns_classification_table` + `test_gap_counts_sum_to_paired` + per-label oracle rows for pure `gap_columns`/`aggregate_gaps` (embedding|fusion|unrecoverable|none|unknown incl. structure reading; bank leg has no split so classification is harness-relative AND computed only on the c-cell — bank-hit/harness-miss — never misattributing agreement as deficit); `unknown` share capped (bar: ≤5%, else the classifier not the data failed); conservation cells sum to paired. Report gains the single gap table (P1 prose consumes it). No new retrieval instrumentation.
- [x] AC3 — Anchor verification never silent. Gate: `test_anchor_verdict_refuse_warn_clean` + `test_stale_anchors_always_recorded` for pure `anchor_verdict`; stale count joins the one-line stdout summary; `results.json` always carries `staleAnchors` even when empty on the success path (on gate failure `main` exits before writing — either write partial results on failure or scope the claim; lane decides, test pins the choice); report repeats the count.
- [x] AC4 — Memory guardrail committed and fail-loud (labelled a backstop, not a guardrail: 2 s sampling cannot catch inter-sample spikes). Gate: `test_memwatch_runs_as_module` + a LIVE-kill proof (spawn a hog under a tiny cap, assert exit 99 + tree death — a pure kill-decision test cannot observe grandchildren/orphans/sampling-gap defects) + a stdout-passthrough test (the probe version clipped child output to its last 6000 chars — C5) for new `scripts/retrieval_tuning/memwatch.py` (import-safe argparse+main, default cap 12 GB, SIGTERM-the-tree, prints peak+cap+RAM; sampling limitation documented in --help). Mutual exclusion: heavy lanes (ingest/eval/refactor-rerun) never run concurrently on one box — 2×12 GB caps exceed it; serialize or gate on joint RSS.

**P3 Post-F1 retrieval-harness consolidation (deduplicate, simplify, compose)**

P3 STATUS (2026-09-10): **DONE** (lane branch off `12a72dfb`; report
`docs/work/2026-09-10-p3-lane-report.md`). AC1: one builder in
`src/retrieval_tuning/scopes.py` + import-only harness shim (identity-gated); the
7-spelling mapping is checked in at `docs/work/p3-merge-map.md` (7 rows, plus the
P1 relocation and the one scratch composition). AC2:
data/{knobs,buckets,graders,corpora}*.json + one `repo_data` reader;
`test_no_hardcoded_knobs.py` derives its forbidden-literal list from the JSON keys
and was watched red on an injected `EVAL_LIMIT = 8`. AC3: thin wrapper +
exit-code contract (0/1/2/3) on fixture banks; on the pinned p1 copy
`project-corpus-100.json` reproduces byte-for-byte, while `eval-set-100.json`
is reported as a smoke regression because that committed artifact predates the
pinned copy and carries no snapshot pin (amendment proposed in the lane report).
AC4: TWO post-refactor quiesced-recipe runs diff CLEAN vs golden C and D under
one tolerance term (exact hashes/hits/contingency/MCC + mean-F1 ±1e-9; only
`sessionId` differs; peaks 6865/6613 MB at cap 12288); full suite 6 failed /
613 passed / 16 skipped with the SAME 6 pre-existing failures as base; the
scripts-harness CI lane simulates green (111 passed / 1 skipped) after a test-only
seam repair (P2's evaluate tests imported chromadb for a provenance monkeypatch —
now a fake-module injection; the one real-Chroma test skips); old-vs-new CLI
parity 5/5 (`P3_CLI_PARITY=1`).

Runs AFTER F1 numbers land for landing (needs the frozen `results.json` golden + corpus snapshot-SHA; also contends for the same HF/Chroma/server resources). Read-only prep (AC1 inventory/merge-map, AC3 fixture-scoped refresh work) is explicitly permitted pre-F1 — it needs no golden and touches no heavy resources. Follow `.ai-badger/skills/scripts-tooling-refactor/SKILL.md`. Deliberate fork excluded: `src/scoring.py` nDCG/MRR vs harness singleton-F1/MCC (different questions).

- [x] AC1 — Duplication merged per corrected inventory (7 bucket/scope-membership spellings: `load_rows` WHERE, `verify_store` WHERE, `_scope_predicate`, probe legs, `_chroma_where`, slice keep-rule, PLUS `scripts/src/retrieval_tuning/corpus.py` `VALID_SCOPES`/`load_corpus` (review F6 — a second table whose validator rejects `custom`; route it through `scopes` or document explicitly why it is off the eval path) → one builder in `src` with SQL+Chroma emitters covering BOTH per-query predicates and ingest-rule set-membership; one `VALID_SCOPES` + default-scope table; one corpus loader; one `scratch_server()` composition). P1's `resolve_buckets` + VALID_SCOPES table move here unchanged in behavior (P1 owns creation, P3 owns relocation — no redefinition). Gate: `pytest scripts/tests` green before+after + checked-in `docs/work/p3-merge-map.md` reviewed against the corrected 7-spelling inventory (a map checked against a 3-count is self-certifying).
- [x] AC2 — JSON owns behavior constants. Gate: `rg` for each moved constant returns zero hits in logic files, enforced by `test_no_hardcoded_knobs.py` whose constant list is DERIVED from the JSON keys (a hand list rots on the next literal). Moves: buckets → `data/buckets.json`; PARAMS/settings defaults/limit/model/bm25 weights → `data/knobs.json`; grader trios → `data/graders.json`; corpus seeds/frames/allowists → `data/corpora/*.json`. (Corpus generators stay two — different contracts — sharing one anchor-resolution lib.)
- [x] AC3 — Refresh script `scripts/refresh-retrieval-corpora.py` (thin wrapper, logic in `src`). Gate: exit-code contract test on fixture banks (0 clean / 1 anchor drift / 2 snapshot mismatch / 3 smoke regression); outputs regenerated corpora + `refresh-report.json`; determinism = fixed SEEDs, stable sorts, no timestamps; anchor re-verification fails loud.
- [x] AC4 — Behavior preservation. Gate: the pre-golden DOUBLE-RUN determinism proof MOVED to P1 close-out C7 (ownership: it gates P1's golden, not P3); P3 keeps only the post-refactor rerun diff vs the frozen golden under ONE tolerance term — exact on hashes/hits/contingency/MCC-null-through, mean-F1 ±1e-9 (viable again once C1 lands; if C1 were rejected the term alternates to anchor-exact + summary-exact) — with `sessionId` + P1/P2 provenance pins (incl. copy `copySnapshotSha256`, C3) allow-listed (NOT `ingestedAt`, which lives in `params.json`, and NOT `staleAnchors`: drift moves paired/hit-rates, so the rerun asserts the frozen corpus snapshot-SHA first and any drift fails as drift, never as refactor noise) + full suite + CI lane + heavy local gate green. Expected values derived by executing legacy code on fixtures, never by reading; old-vs-new CLI parity harness diffs stdout/stderr/exit/artifacts.

**P4 Integration (always last)**

Cross-package joins, checked on the combined tree — parts that pass alone fail together.

- [ ] AC1 — Shared-surface merge order held: `resolve_buckets` lands before P2's `main()` wiring; P2 rebases onto it; P3 rebases onto both; each join re-runs the touched suites on the merged tree (not the lane trees).
- [ ] AC2 — Post-integration full-100 eval (after P3) reproduces the F1 numbers under the single tolerance term of P3 AC4 as amended (exact hashes/hits/contingency/MCC-null-through, mean-F1 ±1e-9 — not a second term; provable only because the P1-close-out C7 double-run fixed the vector-leg nondeterminism) AND carries P2's repeats/spread/gap/anchor blocks — one `results.json` proving both the numbers and the consolidation.
- [ ] AC3 — Fresh-bank end-to-end (P3 AC3 gate): `make_memory_copy.py --verify-only` → refresh script → ingest → evaluate → report green from a clean checkout state, proving the scripted solution a stranger can run next time. Offline/binary assumptions stated up front (1.6 GB cached HF weights + `ai-raccoon` binary present) so a first run fails on wiring, never on download.
- [ ] AC4 — Review-round loop closed: implementation-review MoE findings folded or filed, QA test-honesty verdict recorded, CI green on the PR, Worktree `start` created holds no unmerged/uncommitted work at `finish`.
